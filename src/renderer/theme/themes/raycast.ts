import stylesheetText from './raycast.css?inline';
import type { PresetThemeManifest } from './preset-theme';

export const raycastThemeManifest = {
  id: 'raycast',
  displayName: 'Raycast',
  description: 'Codex-inspired coral red with direct high-energy action',
  stylesheetText,
} satisfies PresetThemeManifest;
