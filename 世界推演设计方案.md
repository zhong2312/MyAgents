# 世界推演设计方案

> 文档状态：设计基线
> 适用范围：MyAgents 小说工作台
> 目标版本：世界推演 V2
> 核心目标：根据小说的世界架构、修炼体系、人物、势力、物品、时间线、正文和剧情工程，推演世界在时间与空间中的动态演化，并让每一个结果可解释、可对比、可重放、可审阅。

## 1. 结论

当前世界推演模块不是完整的世界演化引擎，更接近“按时间顺序播放预设事件”。V2 不应继续在现有事件队列上叠加补丁，而应保留 Repository、快照哈希、运行记录和采纳白名单等可复用能力，重构为：

```text
统一世界时间坐标
+ 空间包含与传播图
+ 人物和势力的受限决策
+ 修炼、资源、寿命与世界法则裁定
+ 剧情上下文和作者约束
= 可持续演化、可解释的候选世界历史
```

V2 为小说工作台原生能力，不依赖 MiroFish、外部 Companion、内嵌网页或额外后台进程。确定性内核使用纯 TypeScript 实现；智能决策统一通过 Workbench Host 的模型场景调用，凭据、供应商和运行时仍由 MyAgents 宿主管理。

## 2. 当前模块审查

### 2.1 已有能力

当前实现主要位于：

- `src/renderer/workbenches/novel/worldSimulationWorldSchema.ts`
- `src/renderer/workbenches/novel/worldSimulationEngine.ts`
- `src/renderer/workbenches/novel/worldSimulationSnapshot.ts`
- `src/renderer/workbenches/novel/worldSimulationWorldRepository.ts`
- `src/renderer/workbenches/novel/worldSimulationAdoption.ts`
- `src/renderer/workbenches/novel/WorldSimulationWorkbench.tsx`

已有能力包括：

- 使用字符串整数和 `BigInt` 语义推进时间。
- 支持下一事件、按天/月/年推进和跳转到指定时间。
- 能从人物库、势力库、设定库空间树和时间线构建基础快照。
- 能选择参与主体、目标区域、种子事件和文本约束。
- 能保存简单世界状态、预排事件和已执行事件。
- 已有基于白名单的人物、势力字段采纳基础。

### 2.2 不可用的根本原因

| 领域 | 当前行为 | 根本问题 |
| --- | --- | --- |
| 时间 | 时间线事件按数组序号映射到第 1、2、3 天 | 没有使用正式历法、`sortKey`、时期和故事时间 |
| 时间跨度 | 月固定为 30 天、年固定为 365 天 | 不支持项目自定义历法，也没有多尺度语义 |
| 超长时间 | 允许填写超大整数 | 没有自适应步长、聚合和纪元叙事，数值大不等于能推演 |
| 空间 | 区域只有层级、活动度、压力和字符串状态 | 没有邻接、距离、交通、人口、资源、军事、文化和传播 |
| 势力 | 势力被压缩为普通 actor | 治理、经济、军事、民望、领土、资产和关系没有进入裁定 |
| 人物 | 只保留状态和意图字符串 | 不会基于目标、知识、性格、寿命、修炼和关系自主行动 |
| 修炼 | 未进入引擎 | 境界、突破、功法、资源、寿命、能力和约束均不生效 |
| 物品 | 未进入快照 | 无法模拟归属、使用、遗失、交易、继承和争夺 |
| 世界规则 | 能读取部分规则文本 | 引擎不执行规则，文本不会约束状态变化 |
| 时间线 | 未来规划可进入预排事件 | 容易把尚未发生的未来计划误当作事实 |
| 剧情工程 | 未接入 | 不知道哪些内容是作者目标、硬护栏或仅供观察 |
| 推演方式 | 只消费已有事件 | 不会自主生成行动、响应、冲突和涌现事件 |
| 解释能力 | 记录字段前后值 | 无法回答事件为何发生、如何传播、受哪条设定约束 |
| UI | 单页事件播放器 | 运行、分析、会商职责混合，缺少连续逻辑链和分支比较 |

### 2.3 现有代码的处置原则

| 现有部分 | V2 处置 |
| --- | --- |
| Repository + `expectedContent` 保存模式 | 保留并扩展 |
| `sourceHash` / 快照来源引用 | 保留，升级为完整基线清单 |
| 字符串整数时间 | 保留概念，替换为正式 `WorldInstant` |
| `world-state.json` 单一可变状态 | 迁移为基线、运行、分支、检查点和事件账本 |
| 通用 `set/add/multiply/trigger` | 仅可作为内核底层操作；对外改为领域命令 |
| 简单事件队列引擎 | 重写为调度、决策、裁定、传播循环 |
| `worldSimulationAdoption.ts` | 保留白名单思想，升级为领域提案和跨库校验 |
| `WorldSimulationWorkbench` | 拆成运行控制台、世界实验室、立场会商三个工作面 |

## 3. 设计目标与原则

### 3.1 统一世界时间坐标

所有事实、事件、状态、人物生命周期、势力周期和观察尺度对齐到同一个世界时间坐标。坐标沿用时间线 `sortKey` 的精神，使用可排序的 bigint 字符串，不受 JavaScript 安全整数限制。

### 3.2 时间尺度决定推演语义

短尺度推演具体行动和事件流；长尺度推演人口、势力、文明、修炼生态和世界法则变化；亿年以上进入纪元叙事，不能把一万亿年实现为逐日循环。

### 3.3 同一分支，多尺度观察

一次运行默认生成一条连续因果链，然后按日、月、年、百年、千年、万年、千亿年、万亿年形成不同观察层。同一事件可以在短尺度显示为具体行动，在长尺度汇总为趋势或里程碑。

只有进行“如果当初……”比较时才创建独立分支。不同分支共享基线但拥有独立状态和随机种子，绝不相互写入。

### 3.4 双层引擎、单一提交入口

- 确定性内核负责时间、空间、状态、硬规则、资源、寿命、传播和冲突裁定。
- 智能决策层负责人物行动、势力策略、社会响应和叙事解释的候选生成。
- 模型无权直接改状态，只能提交结构化候选；候选经内核校验后转换为领域命令。
- 模型不可用时，内核可用规则策略降级运行，并明确标记降级范围。

### 3.5 推演不污染事实

正式事实、推演状态和作者计划必须分开。推演结果始终是候选，不会直接写回人物、势力、时间线、正文或设定库。只有作者审阅采纳后，才通过领域 Repository 生成并应用修改提案。

### 3.6 不全知、不无成本、不瞬移

人物和势力只能基于自己掌握的信息行动；任何行动都受时间、空间、能力、资源、寿命和规则限制。模型不能通过叙事便利绕开内核约束。

### 3.7 每个结果必须可解释

每个事件必须能够追溯到触发条件、行动主体、知识依据、目标、规则、资源、空间路径、对手响应、随机种子、状态变化和后续影响。

## 4. 权威数据与数据分层

### 4.1 数据层级

| 数据层 | 含义 | 生命周期 | 是否属于正式事实 |
| --- | --- | --- | --- |
| 正式事实源 | 人物、势力、设定、物品、时间线、正文等项目文件 | 长期 | 是 |
| 推演投影 | 从正式资料编译出的可计算只读模型 | 随基线重建 | 否 |
| 基线快照 | 某个事实截止点的不可变输入 | 一次运行及其分支共享 | 否 |
| 沙盒状态 | 某个分支正在变化的世界 | 随运行演化 | 否 |
| 推演结果 | 候选事件、趋势、风险和解释链 | 随运行保存 | 否 |
| 采纳提案 | 面向正式领域 Repository 的修改建议 | 审阅期间 | 否 |
| 已采纳正式内容 | 作者确认并成功保存后的领域资料 | 长期 | 是 |

### 4.2 权威等级

```ts
type SimulationAuthority =
  | "actual"        // 已发生事实
  | "canon"         // 已确认世界设定或规则
  | "constraint"    // 作者显式锁定的推演护栏
  | "planned"       // 未来计划，不自动发生
  | "author-secret" // 作者秘密，仅在允许时作为隐藏约束
  | "simulated";    // 推演候选结果
```

基本优先级：

```text
已发生事实
> 世界与修炼硬规则
> 空间可达性、资源、寿命和能力边界
> 作者硬护栏
> 人物与势力决策
> 作者软目标
> 随机性和叙事偏好
```

剧情硬护栏与世界硬规则冲突时，世界规则优先，并产生“剧情不可实现”诊断，不允许通过模型强行圆回去。

## 5. 总体架构

```mermaid
flowchart LR
    A["正式设定与事实时间线"] --> B["世界投影编译器"]
    B --> C["不可变基线快照"]
    C --> D["多尺度时间调度器"]
    D --> E["人物与势力决策"]
    E --> F["规则、资源与冲突裁定"]
    F --> G["空间传播与系统演化"]
    G --> H["状态提交与事件账本"]
    H --> I["多尺度聚合与报告"]
    I --> D
    I --> J["分支比较与采纳提案"]
```

### 5.1 主要组件

| 组件 | 职责 |
| --- | --- |
| `WorldProjectionCompiler` | 将各领域事实源转换为可计算投影，输出诊断与来源引用 |
| `BaselineBuilder` | 锁定项目、事实截止点、章节上下文和所有 `sourceHash` |
| `CalendarAdapter` | 在自定义历法与统一 `sortKey` 之间换算 |
| `SimulationScheduler` | 按下一事件、生命周期、传播到达时间和系统周期自适应跳步 |
| `CharacterDecisionService` | 生成人物候选行动 |
| `FactionDecisionService` | 生成势力候选策略与预算 |
| `WorldProcessService` | 处理人口、经济、生态、灵气、天灾和法则演化 |
| `RuleArbiter` | 校验规则、前置条件、资源、知识、可达性与冲突 |
| `DomainCommandBus` | 通过领域命令原子提交状态变化并记录事件 |
| `PropagationEngine` | 传播军队、人口、信息、文化、灾害和灵气变化 |
| `ScaleAggregator` | 将同一因果链聚合为不同时间尺度的观察结果 |
| `CheckpointStore` | 保存检查点、分支、暂停和恢复数据 |
| `AdoptionProposalBuilder` | 将选中的候选变化转换为可审阅领域提案 |

### 5.2 运行边界

- 确定性内核是纯 TypeScript，可放入 Web Worker，避免长推演阻塞 Renderer。
- 工作区文件仍由绑定项目根目录的 `WorkbenchStorage` 读写，不能绕过宿主文件服务。
- Web Worker 不直接写文件；运行控制器负责检查点和事件分段持久化。
- 智能决策通过 Workbench Host 的模型场景调用，不直接读取宿主凭据或调用供应商 API。
- 工作台不创建第二套聊天、Sidecar 或外部服务生命周期。

## 6. 数据接入与投影编译

| 正式数据源 | 推演投影 | 主要用途 |
| --- | --- | --- |
| `novel.json` | 项目身份、题材和全局参数 | 绑定项目、校验运行归属 |
| 设定库页面与词条 | 世界规则、文化、制度、环境参数 | 规则和区域背景 |
| 空间树、地点库、地图 | 包含图、邻接图、坐标和交通图 | 地域范围、旅行和传播 |
| `world/cultivation-ecology.json` | 修炼体系、境界、寿命、突破、能力、资源和约束 | 修炼及超凡规则裁定 |
| `characters/index.json` | 人物身份、目标、性格、关系、修炼、寿命、物品和位置 | 人物决策与生命周期 |
| `world/factions/index.json` | 治理、军事、经济、民望、领土、资产、关系和组织结构 | 势力策略与兴衰 |
| `world/items/` | 物品能力、状态、位置、所有权和引用 | 交易、使用、遗失、继承和争夺 |
| `timeline/index.json` | 历法、时期、事实事件、因果和事实截止点 | 基线、历史记忆和统一时间坐标 |
| `narrative/index.json` | 主线、故事弧、大纲、章节规划和关联 | 作者意图、护栏和观察指标 |
| `manuscript/index.json`、章节正文 | 已写章节中明确发生的事件 | 可选章节基线或重演目标 |

### 6.1 文本字段的数值化原则

正式资料中大量字段是自然语言。投影编译器可以推导可计算指标，但必须保存：

- 原始文本与稳定来源 ID。
- 推导值及其单位。
- 推导方式：明确值、规则换算、模型推断或默认先验。
- 置信度和缺失状态。
- `sourceHash`，用于检测资料在运行期间是否变化。

推导值只存在于推演投影中，不得反向覆盖作者的正式文本。关键数据缺失时应显示诊断，让作者选择补充资料、接受默认先验或排除该实体。

## 7. 时间系统

### 7.1 核心类型

```ts
interface WorldInstant {
  calendarId: string;
  sortKey: string; // bigint 字符串
  precision: "exact" | "day" | "month" | "year" | "era" | "approximate";
  displayText: string;
}

interface WorldDuration {
  amount: string;
  unit: "tick" | "day" | "month" | "year" | "century" |
    "millennium" | "ten-thousand-years" | "hundred-billion-years" |
    "trillion-years" | "era";
}

interface SimulationWindow {
  start: WorldInstant;
  end?: WorldInstant;
  duration?: WorldDuration;
  outputScales: TimeScale[];
}
```

`month` 和 `year` 是历法单位，不是固定 tick 数。`CalendarAdapter` 根据项目历法处理月份长度、闰置、纪元和非现实世界时间比例。无法精确换算的时间表达保留精度范围，不伪造具体日期。

### 7.2 尺度阶梯

| 尺度 | 调度粒度 | 活跃对象 | 典型内容 |
| --- | --- | --- | --- |
| 日 | 时刻至日 | 人物、队伍、具体地点 | 行动、旅行、修炼、小规模冲突 |
| 月 | 日至月 | 人物、组织、城池 | 战役、密谋、突破、贸易 |
| 年 | 月至年 | 人物、势力、国家 | 国战、政局、外交、人物际遇 |
| 百年 | 年至十年 | 代际、势力、区域 | 领袖换代、宗门兴衰、人口迁移 |
| 千年 | 十年至百年 | 势力、文明、修炼传统 | 王朝更替、传承断层、文明周期 |
| 万年 | 百年至千年 | 文明、生态、世界机制 | 灵气潮汐、地貌和文明更替 |
| 千亿年 | 纪元里程碑 | 世界、法则、宇宙结构 | 法则漂移、恒星和世界格局重组 |
| 万亿年 | 纪元阶段 | 宇宙和终局机制 | 宇宙生灭、法则耗散、世界终局 |

### 7.3 自适应调度

调度器不循环每一天，而是寻找下一个有意义的边界：

- 已开始行动的完成时间。
- 人物修炼、恢复、突破、衰老和寿命边界。
- 势力预算、议事、征税、动员和继承周期。
- 军队、人口、货物、消息和灾害的到达时间。
- 资源耗尽、恢复、生产和再生节点。
- 灵气潮汐、季节、灾变和世界法则周期。
- 作者硬护栏的检查窗口。
- 运行预算规定的检查点。

两个边界之间的静默区间使用数值积分或阶段聚合更新人口、经济、资源、稳定度等连续变量，并记录“静默但发生了哪些缓慢变化”。

### 7.4 生命周期与尺度降级

- 普通人物超过寿命后退出活跃决策，其遗产转化为物品、功法、血脉、制度、传说或文化影响。
- 高境界人物按修炼体系寿命继续存在，不能因进入百年尺度就被统一清除。
- 势力在长尺度下进入兴起、扩张、鼎盛、僵化、衰落、分裂、灭亡或重生状态机。
- 亿年以上，普通个体和短命势力降级为统计群体；仍满足生存规则的特殊主体可保留为纪元级行动者。

### 7.5 多尺度与分支的关系

必须区分两个概念：

- **观察层 `ObservationLayer`**：同一分支的日、月、年、百年等不同摘要，彼此必须因果一致。
- **世界分支 `SimulationBranch`**：不同假设或作者干预产生的独立世界状态，可互相比较但不互相写入。

一次请求可同时选择 1 年、100 年、1 万年作为观察窗口。系统运行到最长终点，并在各窗口生成阶段结果。需要严谨复现某个早期细节时，可从最近检查点创建局部高分辨率重演分支。

## 8. 空间系统

### 8.1 两类空间关系

空间投影同时维护：

1. **包含图**：世界、界域、大陆、国家、城池、宗门、秘境、村庄和具体地点的父子关系。
2. **传播图**：相邻、道路、航线、贸易、传送阵、空间裂隙、信息渠道和其它可达关系。

空间树不能替代邻接图。两个同属一个国家的地点不一定直接可达；两个不同世界的地点也可能通过传送阵瞬时连接。

### 8.2 地域状态

```ts
interface RegionProjection {
  regionId: string;
  parentId: string | null;
  geometry?: RegionGeometry;
  rulers: RegionControl[];
  population: QuantityBand;
  economy: RegionalEconomy;
  military: RegionalMilitary;
  culture: CulturalState;
  cultivation: CultivationEnvironment;
  ecology: EcologicalState;
  resources: ResourceStock[];
  residentActorIds: string[];
  activeFactionIds: string[];
  connections: SpatialConnection[];
  risks: RegionalRisk[];
  sourceRefs: SimulationSourceRef[];
}
```

地域至少覆盖：

- 当前统治者、控制度、直接/自治/傀儡统治方式。
- 多势力交错控制和争议领土。
- 人口量级、经济、粮食、稳定度和社会矛盾。
- 驻军、兵种、动员和防御能力。
- 文化、习俗、信仰、制度和信息接受倾向。
- 灵气浓度、灵脉、秘境、生态和局部法则。
- 关键人物、物品、势力和正在发生的事件。

### 8.3 推演范围

运行配置支持：

- 单个或多个目标地域。
- 选中上级地域并包含指定深度的子地域。
- 包含相邻 N 层或指定旅行时间内的地域。
- 包含贸易、传送或信息可达范围。
- 设置范围外边界：忽略、只响应、统计近似或完整展开。

范围外势力可以对目标区域作出响应，但默认不展开其内部全部细节，以控制计算规模。

### 8.4 空间传播

传播对象包括军队、人物、人口、货物、信息、谣言、文化、瘟疫、灾害和灵气变化。每次传播计算：

```text
起点 + 通道 + 距离 + 速度 + 容量 + 阻断条件
→ 到达时间 + 强度衰减 + 信息失真 + 影响范围
```

地域不是有意志的主体。地域变化由主体行动与人口、资源、生态、文化、军事等系统过程共同形成，再反过来约束下一轮人物和势力行动。

## 9. 人物推演

### 9.1 人物投影

人物推演档案应包含：

- 当前地点、故事时间、年龄、基础寿命、寿命损耗和预计寿命区间。
- 目标、动机、价值观、性格、优势、弱点、恐惧和内在冲突。
- 事实知识、传闻、猜测、误判、秘密和信息来源。
- 人际关系、势力身份、职位、责任、承诺和债务。
- 修炼体系、轨道、境界、功法、能力、资源、约束和突破历史。
- 持有物品、可调用资产、正在执行的计划和未完成行动。

### 9.2 决策循环

```text
感知当前可获得的信息
→ 更新知识、猜测和误判
→ 评估需求、目标与风险
→ 生成候选行动
→ 校验可达性、时间、能力、资源和规则
→ 与其它行动进行冲突裁定
→ 执行领域命令
→ 更新人物、地域和关系状态
→ 安排下一次唤醒时间
```

短尺度输出具体行动、地点、对象和耗时；长尺度输出成长、突破、掌权、隐退、陨落、传承和历史影响。人物死亡可触发继承、遗物争夺、职位空缺、复仇、势力分裂和关系重组。

### 9.3 知识边界

模型输入必须是人物视角的知识投影，不是完整世界快照。人物不能因为作者设定库中存在秘密就自动知道秘密。事件传播到达后才能更新人物知识，谣言还应保留失真和置信度。

## 10. 势力推演

### 10.1 势力投影

势力投影应包含：

- 存续、扩张、复仇、统一、守护、逐利等长期目标。
- 当前战略、短期目标、底线和风险偏好。
- 治理、经济、军事、民望、合法性和领土完整度。
- 成员、领袖、组织单元、派系和继承规则。
- 地盘、资产、人口、灵脉、矿产、物品和其它资源。
- 同盟、敌对、竞争、依附、贸易和历史恩怨。
- 已知情报、误判、外交承诺和可调动预算。

### 10.2 策略池

策略至少包括：

- 战争、动员、扩张、收缩、防御、投降和吞并。
- 结盟、会盟、联姻、背叛、附庸、停战和外交施压。
- 税制、改革、镇压、赈济、迁移、贸易和休养生息。
- 招募、扶植、渗透、暗杀、封锁消息和争夺资源。
- 宗门传承、秘境开发、功法封锁和修炼资源配置。

### 10.3 决策与博弈

```text
局势评估
→ 生成战略候选
→ 预算和可行性校验
→ 对手感知与响应
→ 多方冲突裁定
→ 领土、资源、关系和内部状态变化
→ 长期目标重新评估
```

多势力互动自然生成战争、会盟、贸易、联姻、吞并和代理冲突，不能由模型只写一条不受对手响应的单线故事。

## 11. 修炼、物品与世界过程

### 11.1 修炼体系

修炼体系进入硬规则和生命周期裁定，负责：

- 境界和子阶段前置条件。
- 功法、能力和阵法的适用范围。
- 修炼速度、突破概率、资源消耗和失败代价。
- 寿命增长、寿命损耗、伤势、根基和失衡。
- 灵气、灵脉、秘境和环境依赖。
- 体系克制、跨体系关系和法则冲突。

突破结果不能只由模型宣告。内核先校验条件和资源，再使用固定随机种子裁定，最后由模型生成符合人物经历的表现和解释。

### 11.2 物品

物品作为有稳定 ID 的世界实体参与推演，支持：

- 所有权、保管者、当前位置和知情者。
- 装备、使用、消耗、耐久、封印、损毁和修复。
- 交易、借用、遗失、偷窃、继承和争夺。
- 物品能力对人物、势力、地域和修炼的影响。

### 11.3 世界过程

除主体决策外，世界本身还存在不依赖模型意志的系统过程：

- 人口出生、死亡、迁移和代际更替。
- 经济生产、消费、贸易和资源枯竭。
- 生态恢复、退化、灾害和物种变化。
- 灵气潮汐、灵脉生灭和修炼体系传播。
- 地貌、天体、世界屏障、天道和法则变化。

这些过程由确定性规则和参数驱动；智能层只补充候选社会响应和叙事表达。

## 12. 章节剧情与剧情工程配置

章节正文和剧情工程是两类不同输入：正文描述已经写出的故事，剧情工程描述作者未来的设计。两者不能合并成一个“使用剧情”开关。

### 12.1 章节上下文模式

| 模式 | 基线处理 | 章节内容的语义 | 用途 |
| --- | --- | --- | --- |
| 不使用章节 | 只使用正式时间线事实 | 不进入运行 | 观察世界自然演化 |
| 从章节后继续 | 选择截止章节 | 此前明确事件成为本次沙盒基线 | 推演已写剧情之后的世界 |
| 从章节前重演 | 从目标章节之前开始 | 目标章节是观察项，不保证发生 | 检验剧情能否自然形成 |
| 从章节处分支 | 固定指定章节之前的背景 | 从章节节点生成替代后续 | 平行剧情和改稿比较 |

章节正文不能整段直接提升为事实，需要经过“章节事实编译”：

```text
正文与章节元数据
→ 事件、参与者、地点和故事时间提取
→ 状态变化和因果关系提取
→ 梦境/传闻/谎言/推测/回忆分类
→ 实体引用和时间一致性校验
→ 作者确认或采用“仅明确事实”策略
→ 生成本次运行的章节上下文快照
```

默认只使用明确陈述且高置信度的事实。梦境、传闻、角色误判、不可靠叙述和无法定位时间的回忆不能自动进入沙盒基线。章节编译结果按正文 `sourceHash` 缓存，正文变化后必须失效重建。

### 12.2 剧情工程参与模式

| 模式 | 对推演的影响 |
| --- | --- |
| 关闭 | 不读取剧情工程约束，完全观察世界自然演化 |
| 仅观察 | 不改变决策，只报告结果与剧情设计的符合和偏离 |
| 引导 | 剧情目标参与候选行动评分，但不能突破硬规则 |
| 强约束 | 选中的节点成为护栏，违反时拒绝状态提交或暂停请求处理 |

主线、故事弧、大纲和章节计划必须分别配置：

```ts
interface NarrativeConstraintConfig {
  mode: "off" | "observe" | "guide" | "enforce";
  sources: {
    mainlines: ConstraintSourceConfig;
    storyArcs: ConstraintSourceConfig;
    outlines: ConstraintSourceConfig;
    chapterPlans: ConstraintSourceConfig;
  };
  itemOverrides: Record<string, "disabled" | "observe" | "soft" | "hard">;
}
```

建议默认值：

| 数据来源 | 默认语义 |
| --- | --- |
| 正式时间线事实 | 不可变基线 |
| 已完成章节的明确事件 | 用户启用后成为沙盒基线 |
| 主线 | 软约束 |
| 故事弧 | 软约束 |
| 卷/篇/组大纲 | 仅观察 |
| 章节计划 | 仅观察 |
| 未来时间线事件 | 仅观察，不自动触发 |
| 作者显式锁定节点 | 硬护栏 |
| 伏笔、情绪目标、爽点和商业节点 | 观察指标 |

### 12.3 结构化剧情约束

```ts
interface NarrativeConstraint {
  constraintId: string;
  sourceType: "mainline" | "story-arc" | "outline" | "chapter-plan";
  sourceId: string;
  strength: "observe" | "soft" | "hard";
  timeWindow?: WorldTimeRange;
  requiredActorIds?: string[];
  requiredRegionIds?: string[];
  requiredOutcomes?: StatePredicate[];
  forbiddenOutcomes?: StatePredicate[];
  flexibility: number;
  sourceRefs: SimulationSourceRef[];
}
```

约束不直接生成事件。运行时先生成符合人物和势力立场的候选行动，再由硬护栏过滤、软目标评分，最后记录观察项达成度。

### 12.4 自然演化对照

运行控制台提供“生成无剧情约束对照分支”：

- 自然分支：相同基线和根随机种子，不使用剧情工程约束。
- 剧情分支：使用当前选中的软目标和硬护栏。

世界实验室展示共同事件、首个分歧点、为维持剧情增加的干预、自然达成节点、强制达成节点以及无法成立的设计。这样可以判断剧情来自世界自然演化，还是依赖作者强推。

## 13. 规则、冲突与领域命令

### 13.1 规则类型

| 类型 | 示例 | 行为 |
| --- | --- | --- |
| 世界硬规则 | 无法跨越封闭界域 | 违反即拒绝 |
| 修炼硬规则 | 未满足前置境界不能使用能力 | 违反即拒绝 |
| 过程规则 | 人口、资源、灵气随时间变化 | 内核计算 |
| 主体软规则 | 某人物厌恶背叛 | 影响候选评分 |
| 剧情软目标 | 希望两宗最终开战 | 影响评分并记录偏离 |
| 剧情硬护栏 | 角色在第 50 章前不能死亡 | 拒绝或暂停并报告冲突 |

### 13.2 领域命令

智能层不得提交任意字段补丁，应提交领域命令：

```text
MoveCharacter
StartCharacterPlan
AdvanceCultivation
ApplyInjuryOrDeath
ConsumeResource
TransferItem
ChangeFactionRelation
MobilizeFaction
TransferTerritory
ChangeRegionalControl
ApplyRegionalDisaster
CreateOrDissolveFaction
CreateRumorOrKnowledge
ScheduleFollowUp
```

每个命令负责：

1. 校验目标实体和版本。
2. 校验时间、地点、知识、能力、资源和规则。
3. 计算直接状态变化。
4. 生成事件和因果边。
5. 安排传播、响应和后续唤醒。
6. 原子提交或完整拒绝。

### 13.3 随机性

- 每个运行保存根随机种子。
- 每个命令从稳定标识派生子种子，避免执行顺序的无关变化破坏全部结果。
- 单次运行可精确重放。
- 可选“多样本推演”生成多个分支，用于区分稳定趋势和偶然结果。
- 报告必须区分确定结果、概率结果、模型推断和作者约束造成的结果。

## 14. 推演循环

```text
1. 校验当前基线和 sourceHash
2. 调度到下一个有意义的时间边界
3. 更新静默区间内的连续世界过程
4. 唤醒到期的人物、势力和系统过程
5. 为每个主体构建其可见知识上下文
6. 生成候选行动或使用确定性降级策略
7. 校验规则、空间、资源、寿命和剧情护栏
8. 对并发行动和多方响应进行冲突裁定
9. 通过领域命令提交状态变化
10. 传播影响并安排后续事件
11. 写入事件账本、因果边和检查点
12. 更新各时间尺度观察层和剧情达成度
13. 达到终点、预算或暂停条件后生成报告
```

### 14.1 运行预算

每个方案需要设置或自动估算：

- 最大事件数。
- 最大主体决策次数。
- 最大模型调用数和并发度。
- 最大分支数。
- 检查点间隔。
- 单个尺度允许的最小粒度。

达到预算时应暂停并保存检查点，而不是丢失运行或静默降低质量。

## 15. 智能决策与模型场景

建议注册并使用以下模型场景：

- `simulation.character-decision`
- `simulation.faction-decision`
- `simulation.emergent-response`
- `simulation.epoch-narration`

场景按“场景绑定 → 小说工作台默认模型 → 全局默认模型”解析，由 Host 负责供应商凭据、模型调用、超时和错误。每次输入只装配当前决策需要的局部上下文，避免把完整项目无差别发送给模型。

统一候选输出至少包含：

```ts
interface DecisionProposal {
  proposalId: string;
  actorId?: string;
  objective: string;
  perceivedFacts: string[];
  assumptions: string[];
  proposedCommands: DomainCommand[];
  expectedUtility: number;
  risks: string[];
  confidence: number;
}
```

Schema 解析失败、模型不可用或结果全部被内核拒绝时，使用确定性策略池选择“维持现状、完成已有计划、满足基本生存目标、响应最高威胁”等行动，并在事件和报告中标记 `degradedReason`。

## 16. 推演产出与解释链

### 16.1 事件账本

每个已接受事件记录：

```text
事件 ID 与世界时间
├── 触发条件和父事件
├── 发起主体及当时目标
├── 主体实际掌握的信息
├── 候选行动和选择理由
├── 使用的资源、物品和能力
├── 适用及拒绝的规则
├── 发生地域和传播路径
├── 对手及世界响应
├── 随机种子和概率
├── 领域命令与状态变化
├── 剧情约束影响
└── 后续事件和观察层摘要
```

### 16.2 报告类型

- 世界总览：发生了什么、哪些区域和主体变化最大。
- 多尺度时间摘要：日/月/年事件流、百年趋势、纪元叙事。
- 人物命运：行动、成长、突破、关系和生命周期。
- 势力兴衰：战略、领土、资源、外交和内部危机。
- 空间传播：战争、人口、文化、灾害和灵气变化路径。
- 因果报告：关键事件的前因、响应和连锁后果。
- 剧情报告：主线、故事弧、大纲和章节设计的达成、偏离与冲突。
- 风险报告：数据缺失、低置信推断、规则冲突和降级运行。

### 16.3 多尺度示例

同一条因果链可以显示为：

```text
第 1 日：无显著事件，北山封印按既有速率衰减。
第 10 日：守阵人失踪，封印能量下降速度加快。
第 25 日：灵气外泄影响山村，一名少年首次感应到灵气。
第 8 年：少年加入宗门；两宗开始争夺北山灵脉。
第 100 年：两宗战争重塑北境势力格局，旧王朝失去实际控制。
第 1 万年：灵气潮汐进入低谷，旧修炼体系大面积断代。
第 1000 亿年：世界屏障衰退，多个界域发生长期融合。
第 1 万亿年：法则稳定性进入不可逆衰变，宇宙终局过程开始。
```

每一层都必须能下钻到上一层的来源或说明为何只存在聚合结论。

## 17. 工作面设计

最终导航保留三个入口，但职责彻底分离：

```text
世界推演
├── 运行控制台
├── 世界实验室
└── 立场会商
```

### 17.1 运行控制台

负责回答“从哪里开始、推演什么、如何推演”。

核心配置：

- 基线：正式事实截止点、章节上下文模式和选定章节。
- 时间：起点、终点/持续时长和多个输出尺度。
- 空间：目标地域、子地域深度、邻接范围和外部边界策略。
- 主体：重点人物、势力和是否自动包含相关主体。
- 世界规则：启用规则、缺失参数和默认先验。
- 剧情工程：总模式、主线、故事弧、大纲、章节及单项覆盖。
- 运行：随机种子、预算、模型场景、确定性降级和对照分支。

启动前显示“基线检查”：

- 事实截止点是否明确。
- 章节事实是否已编译且未过期。
- 历法和时间窗口是否可换算。
- 地域是否可达、是否缺少邻接和交通数据。
- 人物、势力和修炼引用是否有效。
- 剧情硬护栏之间是否冲突。
- 预计事件量、模型调用量和运行级别。

运行期间提供启动、暂停、继续、单步到下一边界、创建分支和取消。单步与连续运行必须进入同一个串行执行队列。

### 17.2 世界实验室

负责回答“发生了什么、为什么发生、如果改变条件会怎样”。页面采用同步联动的分析视图，而不是几个互不相关、内容稀少的页签。

建议布局：

- 顶部：世界时间、观察尺度、分支、播放位置和关键状态。
- 左侧：运行/分支树、事件筛选和重点主体。
- 中央：时间轴、空间地图、因果图、状态矩阵和对比视图。
- 右侧：当前事件/主体/地域的证据与状态检查器。

视图联动规则：

- 点击时间轴事件，地图定位地域，因果图高亮前后事件，右侧显示裁定依据。
- 点击地图区域，时间轴筛选该区域，人物和势力状态同步过滤。
- 点击人物或势力，显示其知识、目标、行动、资源和影响范围。
- 切换时间尺度只改变观察粒度，不切换到另一个矛盾世界。
- 分支比较显示首个分歧点、状态差异、剧情达成差异和最小干预。

### 17.3 立场会商

负责回答“各方准备怎么做”，不直接改变世界状态。

- 选择人物、势力、地域代表或作者观察席。
- 每个参与方只能读取自己的知识、目标、底线、资源和误判。
- 支持谈判、战争会议、宗门议事、继承会议和危机响应。
- 展示各方提案、预期收益、风险、对他方的判断和不可接受条件。
- 会商结论可转为运行控制台的“初始干预候选”，经作者确认后进入某个分支。
- 会商记录默认不是事实，也不会直接成为已执行事件。

### 17.4 UI 规范

- 使用项目既有主题 Token、14px UI 字号、稳定栏宽和统一按钮层级。
- 工作台强调信息密度和联动，不使用营销式大标题、装饰性卡片或卡片套卡片。
- 图标按钮使用 Lucide 图标和统一 Tooltip；时间尺度使用分段控制，二元设置使用开关。
- 大量人物、势力、地域和事件列表必须虚拟化，筛选与实体选择器保存稳定 ID。
- 地图、时间轴、因果图和右侧检查器必须有稳定尺寸，动态内容不能导致布局跳动或文字重叠。
- 空态说明缺少什么数据以及可执行动作，不用泛化的“暂无内容”。

## 18. 持久化、分支和重放

建议目录：

```text
simulation/
├── scenarios.json
├── baselines/
│   └── <baseline-id>.json
├── runs/
│   └── <run-id>/
│       ├── manifest.json
│       ├── event-ledger-0001.jsonl
│       ├── causal-edges-0001.jsonl
│       ├── checkpoints/
│       └── reports/
├── branches/
│   └── <branch-id>.json
└── adoption-proposals/
    └── <proposal-id>.json
```

`manifest.json` 至少记录：

- 项目 ID、引擎版本和规则集版本。
- 基线 ID、所有来源路径、`sourceHash` 和事实截止点。
- 章节上下文模式、章节范围和章节提取策略。
- 剧情工程模式、来源选择和单项约束覆盖。
- 时间窗口、输出尺度、空间范围和重点主体。
- 根随机种子、运行预算和模型场景解析结果。
- 当前分支、父分支、状态、检查点和降级信息。

给定以下输入必须能够确定性重放内核接受的状态变化：

```text
baselineHash
+ engineVersion
+ rulesetVersion
+ scenarioConfig
+ rootSeed
+ acceptedDomainCommands
```

模型原始输出用于审计，但重放以经过内核接受的领域命令为准，避免供应商模型变化破坏历史运行。

## 19. 采纳闭环

推演结果不能直接调用 `applyAdoptionToCharacter` 或 `applyAdoptionToFaction` 写入正式资料。V2 采纳流程为：

```text
选择候选事件或状态变化
→ 生成按领域拆分的采纳提案
→ 校验当前正式文件 sourceHash
→ 展示推演基线 / 当前正式内容 / 建议内容
→ 作者逐项选择和编辑
→ 领域 Schema、引用闭合和白名单校验
→ 对应 Repository 原子保存
→ 失败时完整回滚
```

采纳目标可以包括人物、势力、物品、地点、设定、修炼资料和时间线候选。正文不由世界推演自动改写；推演只能生成章节/剧情规划提案或打开完整 Agent Session 辅助创作。

作者还必须选择时间线语义：保存为未来计划、作者秘密，还是已确认发生的事实。推演结果不能自行把 `simulated` 提升为 `actual`。

## 20. 诊断、错误和恢复

### 20.1 基线诊断

按严重度分为：

- 错误：项目身份不匹配、事实截止点不存在、历法无法解析、硬规则循环、基线文件损坏。
- 警告：空间缺少邻接、人物缺少地点、势力缺少资源、修炼寿命不完整、章节事实低置信。
- 提示：未选择剧情约束、外部世界使用统计边界、长尺度将降级为纪元叙事。

错误阻止启动；警告允许用户接受默认先验后继续；提示不阻断。

### 20.2 运行状态

```text
draft → validating → ready → running ↔ paused
                              ├→ completed
                              ├→ cancelled
                              └→ failed
```

- 资料 `sourceHash` 在运行中变化不会修改现有基线，只提示“正式资料已变化”。
- 用户可继续旧基线、从最新资料创建新基线或比较两个基线。
- 暂停后保存检查点；恢复前校验引擎和规则集兼容性。
- 单步、连续运行、取消和恢复共享一个执行队列和 generation fence，避免重复推进。

## 21. 性能策略

- 快照按领域和 `sourceHash` 增量编译。
- 事件账本和因果边分段保存，避免单文件无限增长。
- 使用活跃集合，只唤醒到期或受影响的主体。
- 远距离和低相关区域使用统计模型，不展开每个人物。
- 长尺度使用层级聚合、生命周期状态机和事件驱动跳步。
- 模型调用按地域、势力关系和决策周期批处理，但不同主体仍保持知识隔离。
- UI 只加载当前时间窗口和可见列表，地图和图谱按层级抽样。

## 22. 验收场景

### 22.1 核心场景

从正式时间线事实截止点开始，选择一个国家及其相邻地域，同时观察 1 年、100 年和 1 万年。系统需要做到：

- 至少一个人物和一个势力基于正式设定自主行动。
- 人物行动受地点、知识、修炼、资源和寿命约束。
- 势力行为产生对手响应和区域状态变化。
- 事件通过空间图传播并产生延迟、衰减或失真。
- 1 年、100 年和 1 万年结果来自同一因果分支且相互一致。
- 每个结论可以追溯到来源设定、规则、决策和状态变化。
- 推演结果不会自动写入正式资料。

### 22.2 章节与剧情场景

- 选择“从第 30 章后继续”，章节中的明确事件形成沙盒基线，传闻和梦境不成为事实。
- 选择“从第 30 章前重演”，系统报告该章事件是否能够自然出现以及缺少的前置条件。
- 关闭剧情工程后产生自然分支；启用主线软约束后产生剧情分支，并显示首个分歧点。
- 硬护栏要求人物存活但修炼规则判定其必死时，运行暂停并报告冲突，不能私自延寿。

### 22.3 确定性和安全场景

- 同一基线、版本、配置和种子重放得到相同的内核状态与事件账本。
- 模型不可用时仍可完成降级推演，并明确标记哪些结论来自降级策略。
- 正式资料在运行期间变化不会污染正在运行的分支。
- 一个项目不能读取、控制或采纳另一个项目的运行。
- 未来时间线、章节计划和剧情大纲永远不会被无条件标记为已发生。

## 23. 测试策略

### 23.1 确定性内核

- 自定义历法和 bigint 时间换算属性测试。
- 自适应跳步与逐步执行在相同边界上的状态一致性测试。
- 相同种子重放测试。
- 不同分支状态隔离测试。
- 领域命令不变量和原子拒绝测试。
- 人物寿命、突破、资源消耗和势力生命周期测试。

### 23.2 数据与权威

- `factsThroughEventId` 截止过滤测试。
- `actual/canon/planned/author-secret/simulated` 权威隔离测试。
- 章节事实编译中的梦境、传闻、回忆和误判测试。
- 剧情工程关闭、观察、引导和强约束测试。
- `sourceHash` 漂移、旧基线恢复和跨项目隔离测试。
- 采纳白名单、跨库引用闭合和并发修改冲突测试。

### 23.3 空间与智能层

- 旅行和信息传播时间测试。
- 通道阻断、影响衰减和范围外边界测试。
- 人物知识边界测试，防止模型读取全局秘密。
- 多势力响应与冲突排序测试。
- 模型 Schema 失败、超时和确定性降级测试。

### 23.4 UI

- 运行配置、暂停、恢复、单步和取消工作流测试。
- 时间轴、地图、因果图和检查器联动测试。
- 多尺度切换不改变分支状态测试。
- 自然/剧情分支对比测试。
- 大数据量虚拟列表、桌面/窄屏布局和无重叠截图测试。

## 24. 分阶段实施

### M0：契约和迁移地基

- 定义 V2 Authority、时间、基线、分支、事件、因果和领域命令 Schema。
- 设计 V1 `scenarios.json`、`world-state.json` 的只读兼容和显式迁移策略。
- 建立项目归属、`sourceHash`、版本和重放契约。

验收：能够读取现有方案，但不会把旧预排事件当成 V2 已发生事实。

### M1：投影编译与基线诊断

- 接入完整人物、势力、空间、修炼、物品、时间线、剧情工程和正文索引。
- 实现章节事实编译和剧情约束编译。
- 输出结构化缺失、冲突、低置信和失效引用诊断。

验收：作者能查看一次运行将使用和排除的全部事实、计划与约束。

### M2：时间和空间内核

- 实现统一时间坐标、自定义历法、自适应调度和多尺度观察层。
- 实现包含图、传播图、旅行、信息延迟、影响衰减和范围边界。
- 实现检查点、暂停、恢复和确定性重放。

验收：无模型情况下，世界过程能跨日到万年推进并保持可重放。

### M3：人物、势力与修炼裁定

- 实现人物知识、目标、计划、寿命、修炼和物品状态。
- 实现势力战略、预算、多方响应和兴衰生命周期。
- 实现领域命令、规则裁定和确定性策略池。

验收：一个人物和两个势力能在同一地域产生受规则约束的连续互动。

### M4：智能决策和纪元叙事

- 接入四类模型场景和 Host 模型解析。
- 实现候选 Schema、上下文隔离、失败降级和审计记录。
- 实现亿年以上世界、法则和宇宙级纪元叙事。

验收：模型增强与降级运行都能产出合法状态，模型不能绕过内核规则。

### M5：三个工作面

- 将现有单页拆为运行控制台、世界实验室和立场会商。
- 实现时间轴、地图、因果链、状态矩阵、多尺度观察和分支对比。
- 实现章节上下文、剧情工程约束和自然演化对照配置。

验收：作者能完成“配置 → 运行 → 理解因果 → 比较分支 → 选择结果”的完整流程。

### M6：采纳闭环

- 将推演结果转换为人物、势力、物品、地点、设定、修炼和时间线提案。
- 复用统一提案审阅、三方对比、白名单、引用闭合和 Repository 原子保存。

验收：未经作者确认的推演结果不会改变任何正式资料，发生并发修改时可靠中止。

## 25. 最终完成定义

世界推演 V2 完成的标志不是“能点击开始并生成若干事件”，而是：

1. 世界时间能够按照小说自己的历法持续流动。
2. 不同时间尺度产生不同粒度、但属于同一因果链的结果。
3. 人物、势力和世界过程能够基于正式设定自主产生变化。
4. 空间距离、传播、资源、修炼、寿命和知识边界真实约束行动。
5. 作者可以自由决定是否以已有章节为起点，以及主线、故事弧、大纲和章节计划如何约束推演。
6. 世界实验室能展示事件、地图、主体状态和因果链之间的连续逻辑。
7. 立场会商能够提出各方行动方案，但不会绕过运行控制台直接修改世界。
8. 所有推演结果都是候选，可重放、可分支、可比较、可审阅，不污染正式事实。
