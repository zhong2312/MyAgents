# 世界推演设计方案

> 文档状态：V4 设计基线
> 适用范围：MyAgents 小说工作台
> 架构目标：新版世界推演目标架构（V2 为历史命名）
> 当前契约：V4 数据契约 / `world-kernel-1` 引擎 / `ruleset-1` 规则集（2026-08-19）
> 界面基线：V4 单页文字推演舞台（2026-08-19）
> 核心目标：根据小说的世界架构、修炼体系、人物、势力、物品、时间线、正文和剧情工程，推演世界在时间与空间中的动态演化，并让每一个结果可解释、可对比、可重放、可审阅。

## 0. 已确认的产品决策

本节把讨论中已经确认的产品行为提升为不可歧义的设计决策；后续实现、测试和 UI 评审均以本节及其引用章节为准。

| 决策         | 结论                                                                                                                                                                            |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 入口         | 世界推演在小说工作台“辅助”菜单下只保留“世界推演”一个可见二级入口。点击后直接进入统一工作台，不展示世界推演首页、三级菜单或旧内部页面。                                          |
| 工作面       | 创建、推进、阅读事件、查看状态、会商、因果、分支和采纳都在同一个文字工作面完成；设置是可选弹窗，会商/因果/提案使用抽屉或模态框。                                              |
| 视图         | 不提供空间地图视图。地图仍是独立模块；推演内核继续消费地域、距离、可达性和传播约束，但舞台只呈现文字叙事与结构化状态。                                                          |
| 运行         | 一个项目可以创建多个相互独立的推演，并通过顶部运行切换器即时切换。运行不共享可变状态、随机种子、检查点或事件账本；显式创建的分支才共享不可变基线。                              |
| 轮次         | 一轮是方案中配置的完整时间窗口 `[from, to)`，不是一次模型调用，也不是一段事后拼接的文字。单轮保存前后状态、事件、规则命中、拒绝候选、因果边、传播任务和叙事摘要。               |
| 连续推进     | 连续推演只是串行提交多个轮次，每轮都写检查点并可暂停、查看、分支或取消，不允许把多轮合并成无中间状态的黑盒调用。                                                                |
| 动态世界     | 世界独立于观察对象持续变化。人物、势力、民间生活、资源、战争、遭遇、修炼、出生、死亡和新势力形成均由规则快照与内核裁定；观察对象只改变叙事展开程度。                            |
| 长跨度       | 天、十日、月、季度和年展开具体行动与民生；百年、千年和更长跨度按代际、次数、异常、后果和代表事件聚合；亿年以上进入纪元叙事，禁止逐年或逐人物循环。                              |
| 普通凡人     | 普通凡人没有观察保护。跨越寿命边界时必须裁定有证据的完整人生结局，例如安稳终老、疾病/意外、战争或灾害死亡、颠沛流离后病逝；死亡后镜头转向后代、遗物、家族、地方记忆或制度影响。 |
| 世界架构规则 | 秘境周期、年度节日、王朝战争、资源争夺、人物遭遇和境界升级等设定必须先编译为结构化规则快照，由内核按固定顺序执行；不能由 Renderer 或叙事模型硬编码、猜测或绕过。                |
| 事实边界     | 推演结果永远是 `simulated` 候选，不自动写回人物、势力、世界设定、时间线或正文；作者只能通过审阅提案确认采纳。                                                                   |
| 兼容策略     | V4 不兼容、不迁移、不导入旧世界推演菜单、旧页面、旧运行配置和旧运行数据；无引用旧代码随正式实现删除。                                                                           |

以上决策的详细规则分别见第 13、14、17、18 和 19 节；本节只作为产品层速查，不另行定义第二套语义。

## 1. 结论

当前世界推演模块不是完整的世界演化引擎，更接近“按时间顺序播放预设事件”。V2 不应继续在现有事件队列上叠加补丁，而应保留 Repository、快照哈希、运行记录和采纳白名单等可复用能力，重构为：

```text
统一世界时间坐标
+ 空间包含与传播图
+ 人物和势力的受限决策
+ 修炼、资源、寿命与世界法则裁定
+ 剧情上下文和作者约束
= 可持续演化、可解释的候选世界历史
```

V2 为小说工作台原生能力，不依赖 MiroFish、外部 Companion、内嵌网页或额外后台进程。确定性内核使用纯 TypeScript 实现；智能决策统一通过 Workbench Host 的模型场景调用，凭据、供应商和运行时仍由 MyAgents 宿主管理。

设计判断：以“创建推演 → 选择运行 → 按轮推进 → 阅读世界变化 → 检查实体状态 → 必要时有限干预”为核心循环是正确方向，也更接近文字游戏式的动态世界。必须补上的三个约束是：第一，观察对象只是叙事镜头，不能成为世界状态过滤器；第二，每轮必须先完成结构化世界结算，再生成叙事，不能让模型用一段文字替代状态演化；第三，长跨度必须通过生命周期、周期规则和尺度聚合处理，不能把大跨度拆成无意义的逐日循环。普通凡人的寿命结局、秘境/节日等周期设定、战争与资源争夺、遭遇战、境界升级和新生主体都因此成为内核契约，而不是页面上的装饰选项。

### 当前实现状态与差异（2026-08-19）

当前代码已从旧的“预排事件播放器”迁移到 V4 schema 的运行、基线、分支和事件账本契约。已经可执行的部分包括：

- `WorkbenchStorage` 目录持久化、项目身份和基线来源修订隔离；运行清单记录 `engineVersion`、`rulesetVersion`，版本不兼容时拒绝恢复。
- 事实基线在运行起点按世界时间重放人物生死/地点、势力生命周期和物品地点；未来计划仍保存在 `timelinePlans`，不会进入运行事实。
- 人物、势力、地域、物品、旅行和传播使用领域命令提交；实体、时间、空间路径、数值范围、生命状态、一般资源余额、修炼境界和地域硬规则在内核统一裁定。
- 同一时间边界可按稳定 ID 调度多个可行动人物和势力；模型候选按稳定主体顺序和写入键裁定冲突；世界过程依据地域规则、控制关系和已有状态生成可解释事件，空白地域保持静默。
- 事件、主体决策和模型调用有独立预算；同一时间边界的主体请求按 `maxModelConcurrency` 分批并发，达到预算会保存检查点并暂停。模型候选不可用时，确定性人物/势力策略会写入 `degradedReason`。
- 运行方案支持 `maxBranches` 分支预算（默认 8，包含主分支）；创建假设、自然对照或会商干预分支前统一检查，达到上限会拒绝创建而不改变现有分支。
- 传播方案支持 `maxPropagationHops` 跳数预算（默认 3）；每条空间连接按容量缩放影响和知识载荷，谣言按连接衰减、跳数和稳定种子确定性过滤，并在传播事件中保存载荷审计上下文。
- 长尺度运行保存纪元阶段、聚合周期、人口/文明指数和法则稳定度，并把高尺度推进写成可重放的纪元里程碑事件；这些指数不冒充真实人口统计或宇宙法则模拟。
- 高尺度运行在 `toEnd` 完成后自动生成报告，也可由作者手动重生成；报告流程可调用 `simulation.epoch-narration`，其结果只能引用已记录的纪元事件，并以 `narrative` 报告章节保存；调用失败回退确定性报告并记录降级原因。
- 运行态保存人物长期记忆索引；时钟推进按固定十年半衰期衰减记忆强度，传播排程只携带源主体仍记得且目标人物基线中存在的知识，到达事件再通过领域命令刷新目标人物记忆。寿命、剧情强护栏、自然演化对照、立场会商、分支和确定性账本重放已有可用闭环；采纳提案保留地点、状态变化和因果引用，不直接写正式事实。
- 运行态保存人物关系的好感/信任与生命周期状态、势力关系的外交态度与生命周期状态；人物和势力关系命令经过目标存在性、自身关系、生命/存续状态、数值范围和同一时间写入键校验，确定性敌对势力事件也会提交外交关系变化。
- 已确认事实的采纳提案会把关系变化映射回正式人物关系的 `tone/type/summary` 和正式势力关系的 `kind/direction/status/description`；运行态数值不会伪装成正式资料字段，缺失关系仅以“世界推演关系”或明确的外交关系记录创建。
- 作者约束会进入人物/势力候选和复核提示词；已接受模型事件记录决策审计（目标、感知事实、假设、预期效用、风险）及 Host 原始输出，便于追责和重放诊断。

仍与完整 V2 目标存在差异的部分：跨时间步的并发裁定、复杂多方外交博弈仍未完成；当前关系模型只有单主体、单目标的数值增量，没有关系继承、预算约束、外交承诺履约、谈判多方响应或关系网络级裁定；长期时间尺度尚无真实人口/文明谱系和法则模型；剧情工程已经支持可选时间窗、必需/禁止事件或领域命令谓词、必需主体/地域、灵活度、投影诊断、严格暂停和观察达成度，但逐项模式覆盖和跨层约束冲突解释仍需扩展；正文事实编译已落地第一阶段：只消费正文追踪账本中作者已应用、正文哈希未漂移、逐字证据仍存在、实体引用有效且有可换算世界时间的批次，并按章节模式生成 `chapterFacts`；尚未实现从未结构化正文自动抽取事件、梦境/传闻/回忆的语义分类、跨事实因果合并和章节目标的自然达成评估；统一文字舞台、辅助菜单入口和旧内部路由清理已经按 V4 契约接入，仍需完成全量 UI 验收；采纳领域还需覆盖修炼资料和设定文件，地点与物品已支持正式 Repository 写回但仍缺少更完整的使用效果、市场和争夺链。原型已验证交互方向，不等同于正式产品实现完成。

版本与废弃约定：V2 仅表示架构目标；V3 仅表示历史方案基线，不构成 V4 的数据兼容承诺。V4 只读取按本方案新建的运行；所有旧的单文件世界推演数据、V3 方案和 V3 运行目录都不读取、不迁移、不导入。首次打开 V4 工作台时，如果发现方案或运行索引的 `schemaVersion` 为 V1-V3，Repository 会直接废弃旧推演存储，重建 V4 默认方案和空运行索引，并清理旧运行文件；不提取旧字段、不恢复旧运行。V4 结构损坏、缺失版本或未来版本不会被当作旧版重置，而是直接报告格式错误。正式实现可以复用仍符合 V4 契约的内核与 Repository 能力，但必须重新定义并编译 V4 基线、规则快照和运行清单。

## 2. 当前模块审查

### 2.1 已有能力

当前实现主要位于：

- `src/renderer/workbenches/novel/worldSimulationV2Schema.ts`
- `src/renderer/workbenches/novel/worldSimulationEngineV2.ts`
- `src/renderer/workbenches/novel/worldSimulationProjection.ts`
- `src/renderer/workbenches/novel/worldSimulationRepositoryV2.ts`
- `src/renderer/workbenches/novel/worldSimulationAdoptionV2.ts`
- `src/renderer/workbenches/novel/WorldSimulationWorkbench.tsx`

已落地能力包括：

- 使用字符串整数和 `BigInt` 语义推进时间，支持自定义历法、自适应步长和精确终点。
- 能从人物库、势力库、空间树、修炼体系、物品和时间线构建不可变基线，并按事实截止点重放。
- 运行持久化拆分为基线、分支状态、事件账本、检查点、观察层、议会和报告。
- 人物/势力/地域/物品命令具有统一实体、空间、资源、寿命和硬规则裁定；旅行与传播有延迟、衰减和范围策略。
- 同步调度多个主体，支持剧情分支、自然对照分支、会商干预、严格剧情冲突暂停和预算检查点。
- 采纳走提案与 CAS 审阅链，未来计划、作者秘密和已确认事实保持不同权威语义。

### 2.2 不可用的根本原因

| 领域     | 当前行为                                                                                                                                                                                                                                       | 尚未完成的差异                                                         |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| 时间     | 使用 `WorldInstant`、自定义历法和自适应步长；长尺度保存纪元阶段、周期、人口/文明指数和法则稳定度，并写入纪元里程碑事件                                                                                                                         | 聚合仍是 0-100 指数，尚无真实人口/文明谱系和模型纪元叙事               |
| 空间     | 区域邻接、旅行路径、传播延迟/衰减、范围策略、多跳上限和连接容量已进入裁定；谣言沿途按稳定种子发生可重放失真                                                                                                                                    | 空间统计近似和跨时间步并发传播裁定仍有限                               |
| 势力     | 治理、经济、军事、民望、领土完整度、生命周期和基础外交关系（态度/状态）参与裁定；敌对关系可生成确定性响应，正式采纳可写回外交关系记录                                                                                                          | 预算、税制、继承、外交承诺履约、关系网络继承和多方冲突仍是简化策略     |
| 人物     | 目标、知识、地点、寿命、旅行、一般资源消耗、基础修炼行动和关系变化参与调度；运行态保存长期记忆及好感/信任/关系状态，按固定半衰期衰减，决策提示词按记忆和关系视角隔离，正式采纳可写回人物关系；传播到达可刷新已存在知识并受容量/谣言过滤约束    | 性格/关系网络评分和跨时间步记忆并发裁定尚未完整接入                    |
| 修炼     | 境界顺序、突破资源和硬约束在领域命令层校验                                                                                                                                                                                                     | 功法、能力组合、寿命损耗和复杂生态尚未完整演化                         |
| 物品     | 归属、地点、继承和可见性进入运行状态与提案                                                                                                                                                                                                     | 使用效果、遗失概率、交易市场和争夺链仍有限                             |
| 世界规则 | 地域移动、修炼突破和命令数值边界有内核硬裁定                                                                                                                                                                                                   | 自然语言规则尚未编译为通用 RuleArbiter                                 |
| 时间线   | 事实与计划分离，计划不会进入事实基线                                                                                                                                                                                                           | 章节正文的事实编译仍需提高覆盖率                                       |
| 剧情工程 | 支持观察/引导/严格约束和自然对照分支；可投影时间窗、必需/禁止事件或领域命令、主体/地域范围和灵活度，并在运行报告中计算达成度                                                                                                                   | 逐项模式覆盖、跨层约束冲突解释和正文事实编译仍需扩展                   |
| 推演方式 | 内核可生成世界过程、主体策略、寿命、传播、降级事件和确定性纪元里程碑；同一时间边界按稳定主体顺序和写入键裁定模型候选；`toEnd` 按主体调用 Host 模型场景并按 `maxModelConcurrency` 并发；完成后自动生成报告并可调用 `simulation.epoch-narration` | 跨时间步的并发裁定和复杂多方博弈尚未完成                               |
| 解释能力 | 事件含证据、命令、因果、传播和降级原因；实验室可展示派生因果图                                                                                                                                                                                 | 跨尺度下钻、持久化因果索引和长历史抽样仍未完成                         |
| UI       | V4 原型已验证统一文字舞台、运行切换和右侧状态检查器                                                                                                                                                                                            | 必须按第 17 节完成正式接入、删除旧内部路由与地图视图并通过全量 UI 验收 |

### 2.3 现有代码的处置原则

| 现有部分                                | V2 处置                                                      |
| --------------------------------------- | ------------------------------------------------------------ |
| Repository + `expectedContent` 保存模式 | 保留并扩展                                                   |
| `sourceHash` / 快照来源引用             | 保留，升级为完整基线清单                                     |
| 字符串整数时间                          | 保留概念，替换为正式 `WorldInstant`                          |
| `world-state.json` 单一可变状态         | 迁移为基线、运行、分支、检查点和事件账本                     |
| 通用 `set/add/multiply/trigger`         | 仅可作为内核底层操作；对外改为领域命令                       |
| 简单事件队列引擎                        | 重写为调度、决策、裁定、传播循环                             |
| `worldSimulationAdoption.ts`            | 保留白名单思想，升级为领域提案和跨库校验                     |
| `WorldSimulationWorkbench`              | 重构为单页文字推演舞台；设置、因果、会商与提案使用弹窗或抽屉 |

## 3. 设计目标与原则

### 3.1 统一世界时间坐标

所有事实、事件、状态、人物生命周期、势力周期和观察尺度对齐到同一个世界时间坐标。坐标沿用时间线 `sortKey` 的精神，使用可排序的 bigint 字符串，不受 JavaScript 安全整数限制。

### 3.2 时间尺度决定推演语义

短尺度推演具体行动和事件流；长尺度推演人口、势力、文明、修炼生态和世界法则变化；亿年以上进入纪元叙事，不能把一万亿年实现为逐日循环。

### 3.3 同一分支，多尺度观察

一次运行默认生成一条连续因果链，然后按日、月、年、百年、千年、万年、千亿年、万亿年形成不同观察层。同一事件可以在短尺度显示为具体行动，在长尺度汇总为趋势或里程碑。

只有进行“如果当初……”比较时才创建独立分支。不同分支共享基线但拥有独立状态和随机种子，绝不相互写入。

### 3.4 双层引擎、单一提交入口

- 确定性内核负责时间、空间、状态、硬规则、资源、寿命、传播和冲突裁定。
- 智能决策层负责人物行动、势力策略、社会响应和叙事解释的候选生成。
- 模型无权直接改状态，只能提交结构化候选；候选经内核校验后转换为领域命令。
- 模型不可用时，内核可用规则策略降级运行，并明确标记降级范围。

### 3.5 推演不污染事实

正式事实、推演状态和作者计划必须分开。推演结果始终是候选，不会直接写回人物、势力、时间线、正文或设定库。只有作者审阅采纳后，才通过领域 Repository 生成并应用修改提案。

### 3.6 不全知、不无成本、不瞬移

人物和势力只能基于自己掌握的信息行动；任何行动都受时间、空间、能力、资源、寿命和规则限制。模型不能通过叙事便利绕开内核约束。

### 3.7 每个结果必须可解释

每个事件必须能够追溯到触发条件、行动主体、知识依据、目标、规则、资源、空间路径、对手响应、随机种子、状态变化和后续影响。

## 4. 权威数据与数据分层

### 4.1 数据层级

| 数据层         | 含义                                           | 生命周期             | 是否属于正式事实 |
| -------------- | ---------------------------------------------- | -------------------- | ---------------- |
| 正式事实源     | 人物、势力、设定、物品、时间线、正文等项目文件 | 长期                 | 是               |
| 推演投影       | 从正式资料编译出的可计算只读模型               | 随基线重建           | 否               |
| 基线快照       | 某个事实截止点的不可变输入                     | 一次运行及其分支共享 | 否               |
| 沙盒状态       | 某个分支正在变化的世界                         | 随运行演化           | 否               |
| 推演结果       | 候选事件、趋势、风险和解释链                   | 随运行保存           | 否               |
| 采纳提案       | 面向正式领域 Repository 的修改建议             | 审阅期间             | 否               |
| 已采纳正式内容 | 作者确认并成功保存后的领域资料                 | 长期                 | 是               |

### 4.2 权威等级

```ts
type SimulationAuthority =
  | "actual" // 已发生事实
  | "canon" // 已确认世界设定或规则
  | "constraint" // 作者显式锁定的推演护栏
  | "planned" // 未来计划，不自动发生
  | "author-secret" // 作者秘密，仅在允许时作为隐藏约束
  | "simulated"; // 推演候选结果
```

基本优先级：

```text
已发生事实
> 世界与修炼硬规则
> 空间可达性、资源、寿命和能力边界
> 作者硬护栏
> 人物与势力决策
> 作者软目标
> 随机性和叙事偏好
```

剧情硬护栏与世界硬规则冲突时，世界规则优先，并产生“剧情不可实现”诊断，不允许通过模型强行圆回去。

## 5. 总体架构

```mermaid
flowchart LR
    A["正式设定与事实时间线"] --> B["世界投影编译器"]
    B --> C["不可变基线快照"]
    C --> D["多尺度时间调度器"]
    D --> E["人物与势力决策"]
    E --> F["规则、资源与冲突裁定"]
    F --> G["空间传播与系统演化"]
    G --> H["状态提交与事件账本"]
    H --> I["多尺度聚合与报告"]
    I --> D
    I --> J["分支比较与采纳提案"]
```

### 5.1 主要组件

| 组件                       | 职责                                                   |
| -------------------------- | ------------------------------------------------------ |
| `WorldProjectionCompiler`  | 将各领域事实源转换为可计算投影，输出诊断与来源引用     |
| `BaselineBuilder`          | 锁定项目、事实截止点、章节上下文和所有 `sourceHash`    |
| `CalendarAdapter`          | 在自定义历法与统一 `sortKey` 之间换算                  |
| `SimulationScheduler`      | 按下一事件、生命周期、传播到达时间和系统周期自适应跳步 |
| `CharacterDecisionService` | 生成人物候选行动                                       |
| `FactionDecisionService`   | 生成势力候选策略与预算                                 |
| `WorldProcessService`      | 处理人口、经济、生态、灵气、天灾和法则演化             |
| `RuleArbiter`              | 校验规则、前置条件、资源、知识、可达性与冲突           |
| `DomainCommandBus`         | 通过领域命令原子提交状态变化并记录事件                 |
| `PropagationEngine`        | 传播军队、人口、信息、文化、灾害和灵气变化             |
| `ScaleAggregator`          | 将同一因果链聚合为不同时间尺度的观察结果               |
| `CheckpointStore`          | 保存检查点、分支、暂停和恢复数据                       |
| `AdoptionProposalBuilder`  | 将选中的候选变化转换为可审阅领域提案                   |

### 5.2 运行边界

- 确定性内核是纯 TypeScript，可放入 Web Worker，避免长推演阻塞 Renderer。
- 工作区文件仍由绑定项目根目录的 `WorkbenchStorage` 读写，不能绕过宿主文件服务。
- Web Worker 不直接写文件；运行控制器负责检查点和事件分段持久化。
- 智能决策通过 Workbench Host 的模型场景调用，不直接读取宿主凭据或调用供应商 API。
- 工作台不创建第二套聊天、Sidecar 或外部服务生命周期。

## 6. 数据接入与投影编译

| 正式数据源                        | 推演投影                                           | 主要用途                     |
| --------------------------------- | -------------------------------------------------- | ---------------------------- |
| `novel.json`                      | 项目身份、题材和全局参数                           | 绑定项目、校验运行归属       |
| 设定库页面与词条                  | 世界规则、文化、制度、环境参数                     | 规则和区域背景               |
| 空间树、地点库、地图              | 包含图、邻接图、坐标和交通图                       | 地域范围、旅行和传播         |
| `world/cultivation-ecology.json`  | 修炼体系、境界、寿命、突破、能力、资源和约束       | 修炼及超凡规则裁定           |
| `characters/index.json`           | 人物身份、目标、性格、关系、修炼、寿命、物品和位置 | 人物决策与生命周期           |
| `world/factions/index.json`       | 治理、军事、经济、民望、领土、资产、关系和组织结构 | 势力策略与兴衰               |
| `world/items/`                    | 物品能力、状态、位置、所有权和引用                 | 交易、使用、遗失、继承和争夺 |
| `timeline/index.json`             | 历法、时期、事实事件、因果和事实截止点             | 基线、历史记忆和统一时间坐标 |
| `narrative/index.json`            | 主线、故事弧、大纲、章节规划和关联                 | 作者意图、护栏和观察指标     |
| `manuscript/index.json`、章节正文 | 已写章节中明确发生的事件                           | 可选章节基线或重演目标       |

### 6.1 文本字段的数值化原则

正式资料中大量字段是自然语言。投影编译器可以推导可计算指标，但必须保存：

- 原始文本与稳定来源 ID。
- 推导值及其单位。
- 推导方式：明确值、规则换算、模型推断或默认先验。
- 置信度和缺失状态。
- `sourceHash`，用于检测资料在运行期间是否变化。

推导值只存在于推演投影中，不得反向覆盖作者的正式文本。关键数据缺失时应显示诊断，让作者选择补充资料、接受默认先验或排除该实体。

## 7. 时间系统

### 7.1 核心类型

```ts
interface WorldInstant {
  calendarId: string;
  sortKey: string; // bigint 字符串
  precision: "exact" | "day" | "month" | "year" | "era" | "approximate";
  displayText: string;
}

interface WorldDuration {
  amount: string;
  unit:
    | "tick"
    | "day"
    | "month"
    | "year"
    | "century"
    | "millennium"
    | "ten-thousand-years"
    | "hundred-billion-years"
    | "trillion-years"
    | "era";
}

interface SimulationWindow {
  start: WorldInstant;
  end?: WorldInstant;
  duration?: WorldDuration;
  outputScales: TimeScale[];
}
```

`month` 和 `year` 是历法单位，不是固定 tick 数。`CalendarAdapter` 根据项目历法处理月份长度、闰置、纪元和非现实世界时间比例。无法精确换算的时间表达保留精度范围，不伪造具体日期。

### 7.2 尺度阶梯

| 尺度   | 调度粒度   | 活跃对象             | 典型内容                     |
| ------ | ---------- | -------------------- | ---------------------------- |
| 日     | 时刻至日   | 人物、队伍、具体地点 | 行动、旅行、修炼、小规模冲突 |
| 月     | 日至月     | 人物、组织、城池     | 战役、密谋、突破、贸易       |
| 年     | 月至年     | 人物、势力、国家     | 国战、政局、外交、人物际遇   |
| 百年   | 年至十年   | 代际、势力、区域     | 领袖换代、宗门兴衰、人口迁移 |
| 千年   | 十年至百年 | 势力、文明、修炼传统 | 王朝更替、传承断层、文明周期 |
| 万年   | 百年至千年 | 文明、生态、世界机制 | 灵气潮汐、地貌和文明更替     |
| 千亿年 | 纪元里程碑 | 世界、法则、宇宙结构 | 法则漂移、恒星和世界格局重组 |
| 万亿年 | 纪元阶段   | 宇宙和终局机制       | 宇宙生灭、法则耗散、世界终局 |

### 7.3 自适应调度

调度器不循环每一天，而是寻找下一个有意义的边界：

- 已开始行动的完成时间。
- 人物修炼、恢复、突破、衰老和寿命边界。
- 势力预算、议事、征税、动员和继承周期。
- 军队、人口、货物、消息和灾害的到达时间。
- 资源耗尽、恢复、生产和再生节点。
- 灵气潮汐、季节、灾变和世界法则周期。
- 作者硬护栏的检查窗口。
- 运行预算规定的检查点。

两个边界之间的静默区间使用数值积分或阶段聚合更新人口、经济、资源、稳定度等连续变量，并记录“静默但发生了哪些缓慢变化”。

### 7.4 生命周期与尺度降级

- 普通人物超过寿命后退出活跃决策，其遗产转化为物品、功法、血脉、制度、传说或文化影响。
- 高境界人物按修炼体系寿命继续存在，不能因进入百年尺度就被统一清除。
- 势力在长尺度下进入兴起、扩张、鼎盛、僵化、衰落、分裂、灭亡或重生状态机。
- 亿年以上，普通个体和短命势力降级为统计群体；仍满足生存规则的特殊主体可保留为纪元级行动者。

### 7.5 多尺度与分支的关系

必须区分两个概念：

- **观察层 `ObservationLayer`**：同一分支的日、月、年、百年等不同摘要，彼此必须因果一致。
- **世界分支 `SimulationBranch`**：不同假设或作者干预产生的独立世界状态，可互相比较但不互相写入。

一次请求可同时选择 1 年、100 年、1 万年作为观察窗口。系统运行到最长终点，并在各窗口生成阶段结果。需要严谨复现某个早期细节时，可从最近检查点创建局部高分辨率重演分支。

## 8. 空间系统

### 8.1 两类空间关系

空间投影同时维护：

1. **包含图**：世界、界域、大陆、国家、城池、宗门、秘境、村庄和具体地点的父子关系。
2. **传播图**：相邻、道路、航线、贸易、传送阵、空间裂隙、信息渠道和其它可达关系。

空间树不能替代邻接图。两个同属一个国家的地点不一定直接可达；两个不同世界的地点也可能通过传送阵瞬时连接。

### 8.2 地域状态

```ts
interface RegionProjection {
  regionId: string;
  parentId: string | null;
  geometry?: RegionGeometry;
  rulers: RegionControl[];
  population: QuantityBand;
  economy: RegionalEconomy;
  military: RegionalMilitary;
  culture: CulturalState;
  cultivation: CultivationEnvironment;
  ecology: EcologicalState;
  resources: ResourceStock[];
  residentActorIds: string[];
  activeFactionIds: string[];
  connections: SpatialConnection[];
  risks: RegionalRisk[];
  sourceRefs: SimulationSourceRef[];
}
```

地域至少覆盖：

- 当前统治者、控制度、直接/自治/傀儡统治方式。
- 多势力交错控制和争议领土。
- 人口量级、经济、粮食、稳定度和社会矛盾。
- 驻军、兵种、动员和防御能力。
- 文化、习俗、信仰、制度和信息接受倾向。
- 灵气浓度、灵脉、秘境、生态和局部法则。
- 关键人物、物品、势力和正在发生的事件。

### 8.3 推演范围

运行配置支持：

- 单个或多个目标地域。
- 选中上级地域并包含指定深度的子地域。
- 包含相邻 N 层或指定旅行时间内的地域。
- 包含贸易、传送或信息可达范围。
- 设置范围外边界：忽略、只响应、统计近似或完整展开。

范围外势力可以对目标区域作出响应，但默认不展开其内部全部细节，以控制计算规模。

### 8.4 空间传播

传播对象包括军队、人物、人口、货物、信息、谣言、文化、瘟疫、灾害和灵气变化。每次传播计算：

```text
起点 + 通道 + 距离 + 速度 + 容量 + 阻断条件
→ 到达时间 + 强度衰减 + 信息失真 + 影响范围
```

地域不是有意志的主体。地域变化由主体行动与人口、资源、生态、文化、军事等系统过程共同形成，再反过来约束下一轮人物和势力行动。

当前实现把传播跳数作为方案预算（`maxPropagationHops`，默认 3）；每条连接的 `capacity` 同时限制影响强度和知识载荷规模。事实知识按连接继续传播，`rumor` 知识则以稳定种子、连接衰减和跳数计算可重放的过滤结果；到达事件保存跳数与失真数量，便于审阅和重放。

## 9. 人物推演

### 9.1 人物投影

人物推演档案应包含：

- 当前地点、故事时间、年龄、基础寿命、寿命损耗和预计寿命区间。
- 目标、动机、价值观、性格、优势、弱点、恐惧和内在冲突。
- 事实知识、传闻、猜测、误判、秘密和信息来源。
- 人际关系、势力身份、职位、责任、承诺和债务。
- 修炼体系、轨道、境界、功法、能力、资源、约束和突破历史。
- 持有物品、可调用资产、正在执行的计划和未完成行动。

### 9.2 决策循环

```text
感知当前可获得的信息
→ 更新知识、猜测和误判
→ 评估需求、目标与风险
→ 生成候选行动
→ 校验可达性、时间、能力、资源和规则
→ 与其它行动进行冲突裁定
→ 执行领域命令
→ 更新人物、地域和关系状态
→ 安排下一次唤醒时间
```

短尺度输出具体行动、地点、对象和耗时；长尺度输出成长、突破、掌权、隐退、陨落、传承和历史影响。人物死亡可触发继承、遗物争夺、职位空缺、复仇、势力分裂和关系重组。

### 9.3 知识边界

模型输入必须是人物视角的知识和关系投影，不是完整世界快照。人物不能因为作者设定库中存在秘密就自动知道秘密。V4 运行态以长期记忆索引保存知识强度、首次知道时间和最近回忆时间，以关系目标为键保存好感、信任和关系状态；不读取旧运行的 `knowledgeIds` 字段。时钟推进按固定十年半衰期衰减记忆强度，事件传播到达后才能通过 `character.knowledge` 更新目标人物已有知识；传播载荷受通道容量限制，`rumor` 知识按连接衰减、传播跳数和稳定种子确定性过滤。关系变化必须通过 `character.relation` 命令提交。跨时间步并发裁定仍待完善。

## 10. 势力推演

### 10.1 势力投影

势力投影应包含：

- 存续、扩张、复仇、统一、守护、逐利等长期目标。
- 当前战略、短期目标、底线和风险偏好。
- 治理、经济、军事、民望、合法性和领土完整度。
- 成员、领袖、组织单元、派系和继承规则。
- 地盘、资产、人口、灵脉、矿产、物品和其它资源。
- 同盟、敌对、竞争、依附、贸易和历史恩怨。
- 已知情报、误判、外交承诺和可调动预算。

### 10.2 策略池

策略至少包括：

- 战争、动员、扩张、收缩、防御、投降和吞并。
- 结盟、会盟、联姻、背叛、附庸、停战和外交施压。
- 税制、改革、镇压、赈济、迁移、贸易和休养生息。
- 招募、扶植、渗透、暗杀、封锁消息和争夺资源。
- 宗门传承、秘境开发、功法封锁和修炼资源配置。

### 10.3 决策与博弈

```text
局势评估
→ 生成战略候选
→ 预算和可行性校验
→ 对手感知与响应
→ 多方冲突裁定
→ 领土、资源、关系和内部状态变化
→ 长期目标重新评估
```

多势力互动自然生成战争、会盟、贸易、联姻、吞并和代理冲突，不能由模型只写一条不受对手响应的单线故事。

当前已实现基础外交状态：势力基线关系映射为运行态外交态度和状态；`faction.relation` 只能修改当前势力对存在目标势力的关系，并参与写入键冲突裁定；敌对/竞争关系的确定性策略会产生关系变化事件；确认事实采纳时更新正式关系的状态和描述。当前实现不等同于完整博弈，尚未覆盖关系继承、外交承诺履约、预算/资源谈判、同时多方响应和关系网络级传播。

## 11. 修炼、物品与世界过程

### 11.1 修炼体系

修炼体系进入硬规则和生命周期裁定，负责：

- 境界和子阶段前置条件。
- 功法、能力和阵法的适用范围。
- 修炼速度、突破概率、资源消耗和失败代价。
- 寿命增长、寿命损耗、伤势、根基和失衡。
- 灵气、灵脉、秘境和环境依赖。
- 体系克制、跨体系关系和法则冲突。

突破结果不能只由模型宣告。内核先校验条件和资源，再使用固定随机种子裁定，最后由模型生成符合人物经历的表现和解释。

### 11.2 物品

物品作为有稳定 ID 的世界实体参与推演，支持：

- 所有权、保管者、当前位置和知情者。
- 装备、使用、消耗、耐久、封印、损毁和修复。
- 交易、借用、遗失、偷窃、继承和争夺。
- 物品能力对人物、势力、地域和修炼的影响。

### 11.3 世界过程

除主体决策外，世界本身还存在不依赖模型意志的系统过程：

- 人口出生、死亡、迁移和代际更替。
- 经济生产、消费、贸易和资源枯竭。
- 生态恢复、退化、灾害和物种变化。
- 灵气潮汐、灵脉生灭和修炼体系传播。
- 地貌、天体、世界屏障、天道和法则变化。

这些过程由确定性规则和参数驱动；智能层只补充候选社会响应和叙事表达。

## 12. 章节剧情与剧情工程配置

章节正文和剧情工程是两类不同输入：正文描述已经写出的故事，剧情工程描述作者未来的设计。两者不能合并成一个“使用剧情”开关。

### 12.1 章节上下文模式

| 模式         | 基线处理               | 章节内容的语义               | 用途                   |
| ------------ | ---------------------- | ---------------------------- | ---------------------- |
| 不使用章节   | 只使用正式时间线事实   | 不进入运行                   | 观察世界自然演化       |
| 从章节后继续 | 选择截止章节           | 此前明确事件成为本次沙盒基线 | 推演已写剧情之后的世界 |
| 从章节前重演 | 从目标章节之前开始     | 目标章节是观察项，不保证发生 | 检验剧情能否自然形成   |
| 从章节处分支 | 固定指定章节之前的背景 | 从章节节点生成替代后续       | 平行剧情和改稿比较     |

章节正文不能整段直接提升为事实，需要经过“章节事实编译”：

```text
正文与章节元数据
→ 事件、参与者、地点和故事时间提取
→ 状态变化和因果关系提取
→ 梦境/传闻/谎言/推测/回忆分类
→ 实体引用和时间一致性校验
→ 作者确认或采用“仅明确事实”策略
→ 生成本次运行的章节上下文快照
```

默认只使用明确陈述且高置信度的事实。梦境、传闻、角色误判、不可靠叙述和无法定位时间的回忆不能自动进入沙盒基线。章节编译结果按正文 `sourceHash` 缓存，正文变化后必须失效重建。

当前第一阶段实现约束如下：`chapterFactCompiler` 只读取 `manuscript/state-ledger/` 中状态为 `applied` 的批次，不创建或迁移正文账本；每个批次必须同时通过正文哈希、逐字证据、实体引用、生成时间线投影和正式时间线冲突校验。事实来源保留章节正文与追踪批次的 `sourceRefs`，正文变化后本次编译拒绝旧批次并生成诊断。`after` 将目标章节及之前的有效批次作为沙盒基线；`before` 与 `branch` 只将目标章节之前的批次作为基线，目标章节批次保留为观察候选；`none` 不把章节事实送入运行态。尚未编译出的正文段落不会被模型提示词或内核默认为事实。

正文追踪批次可选保存批次级 `timeAnchorEventId`。该字段只能引用作者已确认目录中的正式时间线事件；章节序号、叙事顺序或模型从自然语言猜出的日期都不能充当世界时间。投影应用时复制锚点的 `worldSortKey`、`sortKey` 和 `timePrecision`；缺少锚点、锚点不存在或锚点时间为 `unknown` 的批次仍可保留在账本中，但其时间线投影保持未定时，章节事实编译会将其排除并给出诊断。当前一个批次只支持一个时间锚点；同一章节包含多个世界时间时必须拆分为多个批次或先建立多个正式时间线事件。旧批次缺省该字段时按未定时语义兼容读取，绝不使用章节序号补齐世界时间。

### 12.2 剧情工程参与模式

| 模式   | 对推演的影响                                         |
| ------ | ---------------------------------------------------- |
| 关闭   | 不读取剧情工程约束，完全观察世界自然演化             |
| 仅观察 | 不改变决策，只报告结果与剧情设计的符合和偏离         |
| 引导   | 剧情目标参与候选行动评分，但不能突破硬规则           |
| 强约束 | 选中的节点成为护栏，违反时拒绝状态提交或暂停请求处理 |

主线、故事弧、大纲和章节计划必须分别配置：

```ts
interface NarrativeConstraintConfig {
  mode: "off" | "observe" | "guide" | "enforce";
  sources: {
    mainlines: ConstraintSourceConfig;
    storyArcs: ConstraintSourceConfig;
    outlines: ConstraintSourceConfig;
    chapterPlans: ConstraintSourceConfig;
  };
  itemOverrides: Record<string, "disabled" | "observe" | "soft" | "hard">;
}
```

建议默认值：

| 数据来源                       | 默认语义               |
| ------------------------------ | ---------------------- |
| 正式时间线事实                 | 不可变基线             |
| 已完成章节的明确事件           | 用户启用后成为沙盒基线 |
| 主线                           | 软约束                 |
| 故事弧                         | 软约束                 |
| 卷/篇/组大纲                   | 仅观察                 |
| 章节计划                       | 仅观察                 |
| 未来时间线事件                 | 仅观察，不自动触发     |
| 作者显式锁定节点               | 硬护栏                 |
| 伏笔、情绪目标、爽点和商业节点 | 观察指标               |

### 12.3 结构化剧情约束

```ts
interface NarrativeConstraint {
  constraintId: string;
  sourceType: "mainline" | "story-arc" | "outline" | "chapter-plan";
  sourceId: string;
  strength: "observe" | "soft" | "hard";
  timeWindow?: {
    startSortKey: string | null;
    endSortKey: string | null;
  };
  requiredActorIds?: string[];
  requiredRegionIds?: string[];
  requiredOutcomes?: NarrativeOutcome[];
  forbiddenOutcomes?: NarrativeOutcome[];
  flexibility?: number; // 0-100；缺省按 50 参与观察评分
  sourceRefs: SimulationSourceRef[];
}

type NarrativeOutcome =
  | {
      id: string;
      kind: "event";
      eventKind: SimulationEventKind;
      entityIds: string[];
      regionIds: string[];
    }
  | {
      id: string;
      kind: "command";
      commandType: WorldDomainCommand["type"];
      entityType: "character" | "faction" | "region" | "item" | null;
      entityId: string | null;
      field: string | null;
      operator: "exists" | "equals" | "contains";
      value: string | number | boolean | null;
    };
```

线路、故事弧、卷篇组目录和章节计划可各自保存上述可选结构化约束；投影时保留原始 `sourceRefs`，缺失主体/地域会生成诊断。约束不直接生成事件：运行时先生成符合人物和势力立场的候选行动，再按事件时间窗匹配谓词。严格模式遇到禁止结果立即暂停，时间窗结束或运行终点未达到必需结果也暂停；观察/引导模式不改变硬裁定，只在报告中记录必需结果、禁止结果命中数和 0-100 达成度。旧剧情工程记录没有该字段时按无结构化结果、灵活度 50 兼容读取。

### 12.4 自然演化对照

运行控制台提供“生成无剧情约束对照分支”：

- 自然分支：相同基线和根随机种子，不使用剧情工程约束。
- 剧情分支：使用当前选中的软目标和硬护栏。

统一推演工作台的因果链或分支比较抽屉展示共同事件、首个分歧点、为维持剧情增加的干预、自然达成节点、强制达成节点以及无法成立的设计。这样可以判断剧情来自世界自然演化，还是依赖作者强推。

## 13. 规则、冲突与领域命令

### 13.1 规则类型

| 类型       | 示例                       | 行为                 |
| ---------- | -------------------------- | -------------------- |
| 世界硬规则 | 无法跨越封闭界域           | 违反即拒绝           |
| 修炼硬规则 | 未满足前置境界不能使用能力 | 违反即拒绝           |
| 过程规则   | 人口、资源、灵气随时间变化 | 内核计算             |
| 主体软规则 | 某人物厌恶背叛             | 影响候选评分         |
| 剧情软目标 | 希望两宗最终开战           | 影响评分并记录偏离   |
| 剧情硬护栏 | 角色在第 50 章前不能死亡   | 拒绝或暂停并报告冲突 |

### 13.1.1 世界架构规则编译

世界架构模块中的设定不能只作为提示词或叙事背景。推演启动时必须把已确认的世界设定、修炼体系、势力资源和时间线事实编译为结构化 `WorldRuleDefinition`，再由内核执行。自然语言原文、设定页路径和词条 ID 只作为 `sourceRefs` 保留，用于审阅和解释，不能在运行中被临时猜测为规则。

```ts
interface WorldRuleDefinition {
  id: string;
  sourceRefs: SourceRef[];
  name: string;
  category:
    | "periodic"
    | "conditional"
    | "lifecycle"
    | "hard-boundary"
    | "world-process";
  enabled: boolean;
  scope: RuleScope;
  priority: number;
  trigger: PeriodicTrigger | ConditionPredicate | LifecycleTrigger;
  effects: RuleEffect[];
  aggregation: AggregationPolicy;
}
```

规则至少覆盖以下可配置类别：

| 类别               | 设定示例                                   | 触发与结算语义                                                         |
| ------------------ | ------------------------------------------ | ---------------------------------------------------------------------- |
| 周期世界事件       | 秘境每 1000 年开启、每年一次节日、灵气潮汐 | 计算时间窗口内命中次数；短窗口展开具体事件，长窗口聚合次数、异常和后果 |
| 势力与制度事件     | 王朝战争、继承危机、征税、宗门议事         | 根据外交、军力、资源压力、合法性、既有承诺和冷却期裁定                 |
| 资源事件           | 灵石矿脉、粮食、商路、秘境名额争夺         | 根据供需、控制权、距离、参与势力和风险阈值裁定，写入资源及关系状态     |
| 人物遭遇战与互动   | 遭遇战、结盟、背叛、交易、婚姻             | 只有存活且可达、知情、具备行动能力的主体可以进入候选池                 |
| 修炼与超凡规则     | 境界升级、走火入魔、寿元变化、功法限制     | 先校验前置境界、资源、环境、伤势和瓶颈，再按稳定种子结算结果           |
| 生命周期与世界过程 | 出生、衰老、死亡、代际、文明兴衰           | 作用于人物、势力和统计群体；不可被叙事偏好绕过                         |

周期规则必须使用世界历法下的精确区间和稳定 ID，而不是 UI 文案或 JavaScript 浮点时间。条件规则必须声明所需状态字段、最短冷却期、适用范围和冲突优先级；缺少输入时输出可行动的基线诊断，不得静默假定规则没有发生。

### 13.1.2 规则优先级与失效主体

同一时间窗口内，内核先按照规则优先级和稳定 ID 排序，再执行可交换的命令批次。默认优先级从高到低为：

```text
生命周期终止与硬边界
> 世界级灾变和强制法则
> 王朝战争与势力存亡
> 资源争夺和领土控制
> 人物遭遇与关系行动
> 境界升级与个人长期计划
> 周期节日和普通民间生活
```

人物死亡、失能、离开范围或知识不足后，必须从后续人物行动、遭遇战和突破候选中移除；势力灭亡、被吞并或失去行动资源后，不能继续执行策略。若同一窗口中死亡、秘境开启和遭遇战相互竞争，生命周期结果先提交，后续不再为失效主体生成行动。无法兼容的硬规则冲突必须暂停该分支并记录冲突，不得凭叙事需要任选一个结果。

### 13.2 领域命令

智能层不得提交任意字段补丁，应提交领域命令：

```text
MoveCharacter
StartCharacterPlan
AdvanceCultivation
ApplyInjuryOrDeath
ConsumeResource
TransferItem
ChangeFactionRelation
MobilizeFaction
TransferTerritory
ChangeRegionalControl
ApplyRegionalDisaster
CreateOrDissolveFaction
CreateRumorOrKnowledge
ScheduleFollowUp
```

每个命令负责：

1. 校验目标实体和版本。
2. 校验时间、地点、知识、能力、资源和规则。
3. 计算直接状态变化。
4. 生成事件和因果边。
5. 安排传播、响应和后续唤醒。
6. 原子提交或完整拒绝。

### 13.3 随机性

- 每个运行保存根随机种子。
- 每个命令从稳定标识派生子种子，避免执行顺序的无关变化破坏全部结果。
- 单次运行可精确重放。
- 可选“多样本推演”生成多个分支，用于区分稳定趋势和偶然结果。
- 报告必须区分确定结果、概率结果、模型推断和作者约束造成的结果。

## 14. 推演循环

```text
1. 校验当前基线、规则集版本、`sourceHash` 和分支检查点。
2. 确定本轮起止时间；单轮恰好跨越方案设置的时间跨度，连续运行重复同一过程。
3. 搜索时间窗口内命中的周期规则，并按聚合策略建立待结算周期事件。
4. 结算人物寿命、伤势、出生、死亡、继承和势力存续等生命周期事件，移除失效主体。
5. 更新静默区间中的连续过程，包括资源、人口、灵气、经济、稳定度和记忆衰减。
6. 唤醒仍可行动的人物、势力及世界过程；为每个主体构建其可见知识上下文。
7. 生成主体行动、势力策略和社会响应候选，或使用确定性降级策略。
8. 结算条件规则，包括王朝战争、资源争夺、灾害、人物遭遇战和领土/关系变化。
9. 校验并结算修炼升级、物品消耗、出生的新人物、新势力及代际承接。
10. 按规则优先级、稳定主体 ID 和写入键裁定冲突；通过领域命令原子提交状态变化。
11. 传播影响并安排后续唤醒；随后写入事件账本、因果边和检查点。
12. 为本轮生成叙事舞台所需的事件摘要和状态快照；观察对象只决定展开程度，不改变世界状态。
13. 更新各时间尺度观察层和剧情达成度；到达 `toEnd` 终点后自动生成报告，预算耗尽或暂停只保存检查点。
```

### 14.1 运行预算

以下运行预算全部由系统根据世界资料、时间跨度、实体规模和当前机器能力自动估算，用户无需设置。高级设置仅允许作者在确有需要时覆盖：

- 最大事件数。
- 最大主体决策次数。
- 最大模型调用数 `maxModelCalls` 和同批主体并发度 `maxModelConcurrency`（1-32）。
- 最大分支数。
- 传播最大跳数 `maxPropagationHops`（0-8，默认 3）。
- 检查点间隔。
- 单个尺度允许的最小粒度。

达到预算时应暂停并保存检查点，而不是丢失运行或静默降低质量。

### 14.2 单轮契约与动态世界结算

“一轮”是作者可理解、可回放、可比较的最小推进单位，不是一次模型调用，也不是把起止时间之间的事件直接拼成一段文字。每轮必须满足以下契约：

```text
输入：分支检查点 + [from, to) 世界时间窗口 + 运行规则快照 + 稳定随机子种子
输出：唯一 RoundRecord
      = 事件集合 + 状态前快照/后快照 + 规则命中 + 被拒绝候选
      + 因果边 + 传播任务 + 待唤醒主体 + 叙事摘要 + 预算消耗
```

- 时间窗口采用左闭右开 `[from, to)`；`to` 时刻发生的事件属于下一轮，只有运行终点才可以将终点事件作为终局结算明确记录。
- 单轮无论跨度是一天还是一百亿年，都必须先完成内核结算，再生成叙事；叙事层不得反过来创造事件、人物、势力或状态。
- 同一轮内的结果按以下结算层分组，层内再按规则优先级、稳定主体 ID 和写入键排序：
  1. **世界时钟与周期命中**：历法、节日、秘境、潮汐、灾变窗口和规则冷却统一命中。
  2. **生命与存续**：出生、衰老、伤势恶化、死亡、继承、势力分裂、吞并和灭亡。
  3. **静默过程**：人口、民生、经济、资源、灵气、生态、稳定度和记忆衰减。
  4. **主体行动**：人物计划、移动、交易、关系、修炼；势力战争、外交、治理、生产和资源争夺。
  5. **冲突与响应**：遭遇战、反击、灾害响应、领土变化、市场变化和社会后果。
  6. **承接与新生**：境界突破、人物/势力生成、代际承接、物品继承、后续计划和传播排程。

- 被拒绝的候选也进入 `RoundRecord`，注明违反的规则和可行的替代路径；不得只保留最终成功故事。
- 没有显著事件的轮次也必须返回静默变化摘要，例如人口增减、资源消耗、节日次数、记忆衰减和稳定度变化，不能返回空白轮次。
- 连续推演只是串行提交多个 `RoundRecord`；每轮都保存检查点并可在任意轮次暂停、查看、创建分支或取消。

### 14.3 周期规则、边界和聚合

周期规则使用世界历法中的稳定起点、周期和时区，不使用 UI 文案或浮点数。规则命中遵循以下约定：

- 计算 `[from, to)` 内的精确命中次数，并保存首个命中、最后一个命中、总次数和被其它规则改变的次数。
- 短跨度（天、十天、月、季度、年）可以展开具体事件；长跨度默认聚合为“发生次数 + 关键异常 + 累积后果 + 可下钻的代表事件”。
- 年度节日在千年窗口中显示为“举行 1000 次，其中战争影响 217 次，形成民间迁徙和物价上涨”等聚合叙事，不生成 1000 个舞台卡片。
- 每 1000 年开启一次的秘境，在跨越边界时必须命中正确次数；秘境开启、关闭、参与资格、资源产出和后续争夺分别记录，不能用一个“秘境事件”吞掉全部后果。
- 同一周期规则的命中不得重复结算；规则重叠时由优先级和 `aggregation` 决定合并、覆盖或拆分，并在解释链中保留原始规则 ID。
- 条件规则必须声明输入字段、最短冷却、适用范围和缺失数据行为。缺少输入时生成诊断并进入降级策略，不能默认为“条件不成立”。

### 14.4 生命周期、普通人物和新生主体

生命周期结果必须先由内核裁定，再由叙事层写成完整的人生或历史结局。普通凡人不是因为被选为观察对象就获得保护：

- 若本轮终点早于寿命边界，人物继续按年龄、健康、资源、关系和环境参与行动。
- 若本轮跨过寿命边界，内核必须在可用证据中裁定具体死因或生存路径：安稳终老、疾病/意外、战争或灾害、遭遇战牵连、饥荒迁徙、被势力处决等；证据不足时只能输出带不确定性的有限结果，不得编造确定细节。
- 人物死亡后从行动、遭遇、修炼、突破和模型候选池移除；其遗物、债务、家族、职位、传闻、仇怨和制度影响可以作为后续事件继续传播。
- 观察对象死亡后，镜头按配置自动切换到后代、遗物、家族、所在地记忆、继承势力或造成的长期后果，并在舞台中明确“镜头已转移”的原因。
- 势力灭亡、被吞并或失去行动资源后不再执行策略；其旧领地、残余成员、制度遗产、技术传承和复兴条件可以继续作为世界状态。
- 新人物、新势力和新制度只能在规则快照允许的出生、组织形成、代际承接或文明演化条件下生成，并拥有稳定 ID、来源事件和初始状态；长尺度只生成统计群体或纪元级主体，不逐个虚构短命个体。

### 14.4.1 普通凡人结局示例

下表是验收叙事的示例，不是固定文案。内核先根据人物状态、地域过程、规则命中和因果证据裁定结果，叙事层再按观察镜头改写为自然语言；没有证据的细节必须保留不确定性。

| 运行条件                                             | 内核需要确认的事实                                          | 舞台可采用的表达方向                                                                 |
| ---------------------------------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| 观察对象是普通凡人，窗口跨越其完整寿命，所在城镇稳定 | 婚姻、子女、职业/资源、健康衰退和自然死亡均有状态或规则依据 | “他后来娶妻生子，在城镇秩序未变的年月里过完一生，年老后安静离世。”                   |
| 观察对象仍是普通凡人，窗口命中城镇灾害或邪修屠戮     | 灾害规则命中、地域受影响、人物在场且没有逃离或保护证据      | “城镇在某次劫难中失守，他与许多居民一同死于这场袭击。”                               |
| 观察对象长期失去住所或资源，窗口未命中直接致死事件   | 迁徙、饥病、关系断裂、资源变化和寿命边界                    | “他辗转多个地方谋生，和亲友逐渐失散，最终在孤独与病痛中终老。”                       |
| 观察对象在窗口中途死亡                               | 死亡时间、死因证据、遗物/家族/职位/传闻承接者               | 先完成死亡前的处境与后果，再明确提示“镜头转移”，继续叙述承接主体，不让死者重新行动。 |

禁止把以上示例退化为“寿命耗尽”“人物消失”或无证据的戏剧化细节；同一状态在不同观察对象下可以详略不同，但不得改变内核已经裁定的事实。

### 14.5 游戏化推演闭环

世界推演的“游戏感”来自作者对连续世界的观察、等待和有限干预，而不是把结果改成自由编故事。每轮工作流固定为：

```text
阅读本轮前情与待处理线索
→ 选择推进 1 轮 / 连续 N 轮 / 暂停查看
→ 接收世界结算与主体行动
→ 阅读关键事件、民间变化和观察对象命运
→ 查看状态、规则命中、被拒绝候选和因果链
→ 可选：观察、投递线索、设置护栏、请求会商、创建对照分支
→ 确认作者候选后才影响下一轮；不确认则保持原分支
```

作者候选不是“输入一句话就直接改世界”，而是带目标、时间、范围、成本和预期效果的领域命令提案。作者可以改变未来条件，不能修改已经完成的轮次；每次干预都必须显示预计影响、可能副作用和首次分歧轮次。

其中“投递线索”是最轻量的作者候选：它以新分支元数据保存为后续候选层的软倾向，既不是当前事实，也不是任何人物已知信息，更不是必然发生的剧情。候选层只有在既有事实、空间、资源、寿命与世界规则均允许时才能采用；线索不会直接写入事件账本、检查点或原分支。作者可在“更多运行操作”中回看当前分支的全部已投递线索。

## 15. 智能决策与模型场景

当前已接入的模型场景为：

- `simulation.actor`：人物主体候选。
- `simulation.world`：势力主体候选。
- `simulation.resolve`：候选复核。
- `simulation.report`：报告编辑。
- `simulation.council`：立场会商。
- `simulation.epoch-narration`：高尺度纪元叙事编辑（`toEnd` 完成后的自动报告和手动报告流程均可调用）。

已实现：运行态长期记忆索引、固定半衰期遗忘、按跳数上限的多跳传播、连接容量裁定、谣言稳定失真、人物/势力基础关系变化、同一时间边界的稳定写入键冲突裁定和拒绝警告；作者约束进入主体/复核提示词，已接受模型事件保存结构化决策审计和原始模型文本。尚待实现：跨时间步并发裁定、关系继承与复杂多方博弈。`simulation.epoch-narration` 已在自动/手动报告流程注册并校验输出；主体模型请求已按 `maxModelConcurrency` 做同批并发，并由 `maxModelCalls` 统一计数。

场景按“场景绑定 → 小说工作台默认模型 → 全局默认模型”解析，由 Host 负责供应商凭据、模型调用、超时和错误。每次输入只装配当前决策需要的局部上下文，避免把完整项目无差别发送给模型。

统一候选输出至少包含：

```ts
interface DecisionProposal {
  proposalId: string;
  actorId?: string;
  objective: string;
  perceivedFacts: string[];
  assumptions: string[];
  proposedCommands: DomainCommand[];
  expectedUtility: number;
  risks: string[];
  confidence: number;
}
```

Schema 解析失败、模型不可用或结果全部被内核拒绝时，使用确定性策略池选择“维持现状、完成已有计划、满足基本生存目标、响应最高威胁”等行动，并在事件和报告中标记 `degradedReason`。

## 16. 推演产出与解释链

### 16.1 事件账本

每个已接受事件记录：

```text
事件 ID 与世界时间
├── 触发条件和父事件
├── 发起主体及当时目标
├── 主体实际掌握的信息
├── 候选行动和选择理由
├── 使用的资源、物品和能力
├── 适用及拒绝的规则
├── 发生地域和传播路径
├── 对手及世界响应
├── 随机种子和概率
├── 领域命令与状态变化
├── 剧情约束影响
└── 后续事件和观察层摘要
```

### 16.2 报告类型

- 世界总览：发生了什么、哪些区域和主体变化最大。
- 多尺度时间摘要：日/月/年事件流、百年趋势、纪元叙事。
- 人物命运：行动、成长、突破、关系和生命周期。
- 势力兴衰：战略、领土、资源、外交和内部危机。
- 空间传播：战争、人口、文化、灾害和灵气变化路径。
- 因果报告：关键事件的前因、响应和连锁后果。
- 剧情报告：主线、故事弧、大纲和章节设计的达成、偏离与冲突。
- 风险报告：数据缺失、低置信推断、规则冲突和降级运行。

### 16.3 多尺度示例

同一条因果链可以显示为：

```text
第 1 日：无显著事件，北山封印按既有速率衰减。
第 10 日：守阵人失踪，封印能量下降速度加快。
第 25 日：灵气外泄影响山村，一名少年首次感应到灵气。
第 8 年：少年加入宗门；两宗开始争夺北山灵脉。
第 100 年：两宗战争重塑北境势力格局，旧王朝失去实际控制。
第 1 万年：灵气潮汐进入低谷，旧修炼体系大面积断代。
第 1000 亿年：世界屏障衰退，多个界域发生长期融合。
第 1 万亿年：法则稳定性进入不可逆衰变，宇宙终局过程开始。
```

每一层都必须能下钻到上一层的来源或说明为何只存在聚合结论。

### 16.4 文字舞台叙事契约

舞台文本采用“事实结果在前、叙事表达在后”的两层结构：

1. `RoundRecord` 先生成结构化事件、状态变化、证据、规则和因果关系。
2. 叙事生成器只允许改写顺序、视角、详略和语言，不得增加未被事件账本支持的事实。
3. 每个关键段落必须可回指一个或多个事件 ID；聚合段落必须标明统计窗口、聚合规则和代表性证据。
4. 普通人物的跨年人生必须至少呈现“处境变化 → 关键选择或外部冲击 → 关系/民生后果 → 结局或镜头转移”，不能只显示“已死亡”。
5. 对于被拒绝、未知或概率性结果，舞台使用“未发生 / 可能发生 / 因资料不足无法判断”等明确措辞，不把猜测写成事实。

## 17. 新版推演工作台

本节替代此前“运行控制台、世界实验室、立场会商”三个内部页面及地图联动的 UI 约定。世界推演是小说工作台 **辅助** 菜单下的一个二级入口；点击“世界推演”直接进入统一工作台，不再展示世界推演的三级菜单、首页或空间地图视图。空间、距离、地域和传播仍是内核约束，但不作为本工作台的可视化中心。

### 17.1 页面与运行切换

进入入口后遵循以下优先级：

1. 有当前活动运行时，直接打开该运行及其当前轮次。
2. 没有活动运行但存在运行时，打开最近使用的运行并允许立即切换。
3. 没有可用运行时，直接打开三栏“事实基线”工作面：左侧保留第 0 轮基线，中间显示中心空舞台和“创建推演”动作，右侧展示已编译实体；不展示空白菜单页或强制弹窗。作者可以主动打开可选设置，但不打开也能创建。

同一项目可以保存多个独立推演。顶部标题是运行切换器，展开后按主运行及其分支分组显示名称、基线、总范围、每轮跨度、当前时间和状态；选择一个运行立即替换整页的时间线、舞台和状态快照。新建运行从同一切换器和页面顶部命令进入。运行之间绝不共享可变状态、随机种子、检查点或事件账本；只有显式创建的分支共享不可变基线。

### 17.2 自动创建运行

创建不是必填设置流程。无运行时的空舞台和顶部运行切换器中的“新建”都直接创建一个运行，随后切换到该运行的第 0 轮事实基线。系统按已有运行数自动命名为“自动推演 1、自动推演 2……”，作者通过运行切换器区分和切换它们，无需命名。

总范围、每轮跨度和叙事镜头由运行默认策略自动给出，作为冻结快照的一部分；当前起步策略为总范围十二年、每轮三个月、多主体世界。工作面提供可选设置弹窗，仅允许作者主动调整名称、总范围、每轮跨度和叙事镜头；不打开弹窗也可以直接创建。需要观察某一事件的后续时，作者在舞台中继续单轮或连续推演；需要产生另一种可能性时，从事件创建分支或新建独立推演。

正式事实截止点、可观察地域和主体、人物初始地点、世界架构规则、剧情工程开关、随机种子、预算和确定性降级策略均由创建时的世界投影自动推导并冻结到运行快照。资料缺失时从世界日 0 或可得的最早事实开始，并在舞台中以信息提示说明近似处理；人物地点无法推导时保留为未知，仍可参与生命周期、关系与非空间行动，只跳过移动和地域遭遇等需要明确地点的候选。不得要求作者为了创建推演填写任何字段。仅在作者从舞台主动设置护栏、投递线索或创建分支时，才创建新的干预分支保存该候选，不回写原运行或已完成轮次。

普通凡人的寿命处理由内核根据当前状态和时间跨度裁定；不得因观察镜头选择而延长其寿命或阻止其死亡。

### 17.3 统一文字推演舞台

工作台为稳定的三栏布局，不使用空间地图：

```text
顶部：运行切换器、当前世界时间、每轮跨度、单轮/连续推演与运行操作
左侧：纵向推演时间线
中间：文字叙事舞台 + 本轮行动记录
右侧：当前时间点实体状态检查器
```

**左侧时间线** 按轮次显示事实基线、已完成轮次、运行中轮次和待推演轮次。每项显示时间窗口、事件数和状态。选择历史轮次只能改变观察位置，不能回写当时状态；从历史轮次改变条件时必须显式创建分支。

**中间舞台** 展示选定轮次的世界叙事，而不是地图或静态事件表。舞台包含当前场景时间、事件标题、叙事正文、触发规则、行动主体、裁定状态和证据标签；“世界与民生 / 人物行动 / 势力博弈 / 生命与代际”四类脉动均可直接切换到对应状态标签。本轮行动记录按世界时间和稳定序号显示已接受事件，点击后同步舞台叙事与右侧状态。作者可从当前事件打开因果链、生成采纳提案，或记录“观察、投递线索、设置护栏、创建对照分支”等作者干预候选。作者候选只在确认进入分支后影响下一次裁定，永不直接修改正式事实或已完成轮次。

**右侧检查器** 始终对应所选轮次结束时的状态快照，使用人物、势力、地域、世界四个稳定标签：

- 人物：生命状态、位置、意图、已知信息、资源、关系、境界、突破进度、伤势和寿命。
- 势力：存续状态、策略、军力、经济、民望、资源、外交、领土完整度和继承风险。
- 地域：稳定度、资源、灵气、生态、人口、风险和正在生效的世界规则。
- 世界：当前时间、纪元/文明指标、法则稳定度、已触发规则次数、资源压力、预算和降级原因。

点击事件时，舞台与检查器切换到该事件的主体和裁定依据；涉及该事件的势力与地域在状态列表中必须被突出标记。点击检查器实体只改变展开焦点。因果链、分支比较和立场会商作为抽屉或模态分析工具打开，不再拥有独立导航页面。会商产物仍然只是作者干预候选。

### 17.4 推进与长跨度呈现

“推演 1 轮”只推进方案设置的一个时间窗口；“连续推演”允许作者设置轮数，按同一个串行执行队列逐轮结算、逐轮保存检查点。连续运行不得把多轮合并为一次无中间状态的黑盒调用。运行中页面显示正在结算的轮次，取消、暂停和恢复都只作用于该运行。

舞台在运行中还要显示与控制器真实阶段一一对应的轮内结算流：固定时间边界与已锁定基线、推演人物/势力/民间行动、裁定规则/资源/境界/冲突与传播、写入事件账本和状态检查点。阶段流只投影当前操作的实际进度，不显示虚构百分比，也不写入分支状态、事件账本或检查点；完成、取消或失败后必须清除，不能影响确定性重放。

时间跨度决定叙事颗粒度，不改变同一分支的因果链：

| 单轮跨度           | 舞台重点                                                 |
| ------------------ | -------------------------------------------------------- |
| 日、十日、月、季度 | 具体人物行动、局部世界波动、市场与民间日常               |
| 年、十年           | 人物目标变化、势力策略、资源秩序、节庆和一代人的生活习惯 |
| 百年、千年         | 生命周期结局、代际、势力兴衰、制度、战争阶段、秘境与传承 |
| 万年至百万年       | 文明、修炼生态、地貌和世界机制的阶段性重构               |
| 亿年以上           | 纪元、法则、世界边界和终局过程；只产生可追溯的尺度级结论 |

周期事件必须聚合。例如 1000 年窗口中，年度节日应输出“举行 1000 次、其中多少次受战争影响以及形成何种民间后果”，而不是产生 1000 条节日事件；1000 年一开的秘境则应输出开启次数、参与势力、关键后果和后续影响。百亿年窗口不得逐人物、逐年或逐节日模拟。

### 17.5 观察镜头与普通人物

世界状态独立于观察对象推进。观察对象只决定舞台如何分配叙事预算：被观察对象展开完整行动、关系、选择和结局；非观察对象只保留影响当前叙事所需的摘要。普通凡人跨越寿命边界时，内核应根据已裁定状态形成完整的人生结局，例如安稳终老、死于城镇劫难或颠沛流离后病逝；不得只写“寿命耗尽”。

观察对象死亡后，镜头自动转向其后代、遗物、家族、制度影响或地方记忆。死亡人物不再进入行动、遭遇、修炼或升级候选。该规则同样适用于灭亡势力：叙事可以保留其遗产，但不能继续让其以存续主体行动。

### 17.6 UI 规范与退出项

- 使用项目既有主题 Token、14px UI 字号、稳定栏宽和统一按钮层级；页面强调文字叙事、扫描效率和重复操作效率。
- 工具按钮使用 Lucide 图标与 Tooltip；有明确命令的控件保留文字按钮。运行切换使用菜单，观察类型使用标签，连续轮数使用数字输入。
- 时间线、舞台、检查器、事件流和图标按钮必须有稳定尺寸；窄屏切换为“轮次 / 舞台 / 状态”三个视图，文本不得重叠或截断关键信息。
- 大量运行、轮次、实体和事件列表必须虚拟化，所有引用保存稳定 ID。
- 旧的世界推演子菜单、独立世界实验室页面、独立立场会商页面、空间地图 DOM/样式/交互与对应无引用代码均应删除，不提供旧 UI 兼容层或迁移入口。
- 地图仍是小说工作台独立一级模块；世界推演需要空间约束时通过其投影读取，不在推演页复刻地图编辑或浏览体验。

### 17.7 正式实现交接

V4 原型仅用于确认工作流和信息层级，原型中的人物、势力、规则、随机结果与文案不得作为正式运行数据。正式实现必须遵守以下边界：

1. 运行切换、轮次选择、事件筛选和检查器焦点是 UI 状态；世界时间、轮次结果、实体快照、规则命中和因果边是 Repository 持久化的运行状态。
2. 创建运行时从正式 Repository 编译基线和规则快照；不得在 `WorldSimulationWorkbench` 或 Renderer 中硬编码秘境、节日、战争、资源争夺、人物或境界规则。
3. `observer`、时间范围、每轮跨度、范围、剧情约束、预算、随机种子和规则集版本是运行配置的一部分，必须随运行保存并参与重放。
4. 事件叙事只能基于已接受事件、状态变化、规则命中和证据生成；模型生成的文本不能新增未经裁定的事实。
5. V4 替换旧工作面时删除 `simulation-console`、`simulation-lab`、`simulation-council` 路由及其导航、组件和无引用样式；不为旧 UI、旧运行配置或旧运行数据保留兼容跳转、读取或迁移。V4 只接受按本方案重新创建、重新编译基线和规则快照的运行；旧文件可由作者自行备份，但不属于正式推演功能范围。

## 18. 持久化、分支和重放

建议目录：

```text
simulation/
├── scenarios.json
├── baselines/
│   └── <baseline-id>.json
├── runs/
│   └── <run-id>/
│       ├── manifest.json
│       ├── event-ledger-0001.jsonl
│       ├── causal-edges-0001.jsonl
│       ├── checkpoints/
│       └── reports/
├── branches/
│   └── <branch-id>.json
└── adoption-proposals/
    └── <proposal-id>.json
```

`manifest.json` 至少记录：

- 项目 ID、引擎版本和规则集版本。
- 基线 ID、所有来源路径、`sourceHash` 和事实截止点。
- 章节上下文模式、章节范围和章节提取策略。
- 剧情工程模式、来源选择和单项约束覆盖。
- 时间窗口、每轮跨度、输出尺度、空间范围、重点主体和叙事观察对象。
- 编译规则快照的稳定引用、规则来源 `sourceRefs`、规则集版本和聚合策略版本。
- 根随机种子、运行预算和模型场景解析结果。
- 当前分支、父分支、状态、检查点和降级信息。

给定以下输入必须能够确定性重放内核接受的状态变化：

```text
baselineHash
+ engineVersion
+ rulesetVersion
+ scenarioConfig
+ rootSeed
+ acceptedDomainCommands
```

模型原始输出用于审计，但重放以经过内核接受的领域命令为准，避免供应商模型变化破坏历史运行。

## 19. 采纳闭环

推演结果不能直接调用 `applyAdoptionToCharacter` 或 `applyAdoptionToFaction` 写入正式资料。V2 采纳流程为：

```text
选择候选事件或状态变化
→ 生成按领域拆分的采纳提案
→ 校验当前正式文件 sourceHash
→ 展示推演基线 / 当前正式内容 / 建议内容
→ 作者逐项选择和编辑
→ 领域 Schema、引用闭合和白名单校验
→ 对应 Repository 原子保存
→ 失败时完整回滚
```

采纳目标可以包括人物、势力、物品、地点、设定、修炼资料和时间线候选。正文不由世界推演自动改写；推演只能生成章节/剧情规划提案或打开完整 Agent Session 辅助创作。

作者还必须选择时间线语义：保存为未来计划、作者秘密，还是已确认发生的事实。推演结果不能自行把 `simulated` 提升为 `actual`。

## 20. 诊断、错误和恢复

### 20.1 基线诊断

按严重度分为：

- 错误：项目身份不匹配、事实截止点不存在、历法无法解析、硬规则循环、基线文件损坏。
- 警告：空间缺少邻接、人物地点未知、势力缺少资源、修炼寿命不完整、章节事实低置信。警告只在舞台中说明近似处理，不能要求作者补齐后才能启动。
- 提示：未选择剧情约束、外部世界使用统计边界、长尺度将降级为纪元叙事。

错误阻止启动；警告自动采用明确的降级语义后继续；提示不阻断。

### 20.2 运行状态

```text
draft → validating → ready → running ↔ paused
                              ├→ completed
                              ├→ cancelled
                              └→ failed
```

- 资料 `sourceHash` 在运行中变化不会修改现有基线，只提示“正式资料已变化”。
- 用户可继续旧基线、从最新资料创建新基线或比较两个基线。
- 暂停后保存检查点；恢复前校验引擎和规则集兼容性。
- 单步、连续运行、取消和恢复共享一个执行队列和 generation fence，避免重复推进。

## 21. 性能策略

- 快照按领域和 `sourceHash` 增量编译。
- 事件账本和因果边分段保存，避免单文件无限增长。
- 使用活跃集合，只唤醒到期或受影响的主体。
- 远距离和低相关区域使用统计模型，不展开每个人物。
- 长尺度使用层级聚合、生命周期状态机和事件驱动跳步。
- 模型调用按地域、势力关系和决策周期批处理，但不同主体仍保持知识隔离。
- UI 只加载当前时间窗口和可见列表；因果链和历史比较按需加载，空间数据只在内核和事件证据中按需解析。

## 22. 验收场景

### 22.1 核心场景

从正式时间线事实截止点开始，选择一个国家及其相邻地域，同时观察 1 年、100 年和 1 万年。系统需要做到：

- 至少一个人物和一个势力基于正式设定自主行动。
- 人物行动受地点、知识、修炼、资源和寿命约束。
- 势力行为产生对手响应和区域状态变化。
- 事件通过空间图传播并产生延迟、衰减或失真。
- 1 年、100 年和 1 万年结果来自同一因果分支且相互一致。
- 每个结论可以追溯到来源设定、规则、决策和状态变化。
- 推演结果不会自动写入正式资料。

### 22.2 章节与剧情场景

- 选择“从第 30 章后继续”，章节中的明确事件形成沙盒基线，传闻和梦境不成为事实。
- 选择“从第 30 章前重演”，系统报告该章事件是否能够自然出现以及缺少的前置条件。
- 关闭剧情工程后产生自然分支；启用主线软约束后产生剧情分支，并显示首个分歧点。
- 硬护栏要求人物存活但修炼规则判定其必死时，运行暂停并报告冲突，不能私自延寿。

### 22.3 确定性和安全场景

- 同一基线、版本、配置和种子重放得到相同的内核状态与事件账本。
- 模型不可用时仍可完成降级推演，并明确标记哪些结论来自降级策略。
- 正式资料在运行期间变化不会污染正在运行的分支。
- 一个项目不能读取、控制或采纳另一个项目的运行。
- 未来时间线、章节计划和剧情大纲永远不会被无条件标记为已发生。

## 23. 测试策略

### 23.1 确定性内核

- 自定义历法和 bigint 时间换算属性测试。
- 自适应跳步与逐步执行在相同边界上的状态一致性测试。
- 相同种子重放测试。
- 不同分支状态隔离测试。
- 领域命令不变量和原子拒绝测试。
- 人物寿命、突破、资源消耗和势力生命周期测试。

### 23.2 数据与权威

- `factsThroughEventId` 截止过滤测试。
- `actual/canon/planned/author-secret/simulated` 权威隔离测试。
- 章节事实编译中的梦境、传闻、回忆和误判测试。
- 剧情工程关闭、观察、引导和强约束测试。
- `sourceHash` 漂移、旧基线恢复和跨项目隔离测试。
- 采纳白名单、跨库引用闭合和并发修改冲突测试。

### 23.3 空间与智能层

- 旅行和信息传播时间测试。
- 通道阻断、影响衰减和范围外边界测试。
- 人物知识边界测试，防止模型读取全局秘密。
- 多势力响应与冲突排序测试。
- 模型 Schema 失败、超时和确定性降级测试。

### 23.4 UI

- 新建运行、运行切换、分支分组、无配置创建、暂停、恢复、单轮、连续推演和取消工作流测试。
- 左侧轮次、中央文字舞台、事件流、因果抽屉和右侧状态检查器的同步测试。
- 一天、一年、一千年、百亿年跨度下的叙事粒度与状态一致性测试。
- 周期规则聚合测试：年度节日在千年窗口只产生聚合事件，千年秘境正确命中窗口边界。
- 人物/势力失效测试：人物死亡后不进入遭遇与突破候选，势力灭亡后不继续执行策略。
- 多尺度切换不改变分支状态、自然/剧情分支对比、虚拟列表、桌面/窄屏布局和无重叠截图测试。

## 24. 分阶段实施

### M0：契约和数据地基

- 定义 V2 Authority、时间、基线、分支、事件、因果和领域命令 Schema。
- V4 schema 固化引擎/规则集版本、每轮跨度、观察对象、规则快照和预算字段；旧 `scenarios.json`、`world-state.json` 及旧运行目录不在任何打开流程中读取、迁移或导入。
- 建立项目归属、`sourceHash`、版本和重放契约。

验收：运行清单拒绝不兼容的 schema、引擎或规则集版本；新运行只能由正式资料重新编译，旧预排事件永远不会被当成已发生事实。

### M1：投影编译与基线诊断

- 接入完整人物、势力、空间、修炼、物品、时间线、剧情工程和正文索引。
- 实现章节事实编译第一阶段和剧情约束编译；正文追踪批次可按章节模式进入基线，未应用、证据失效、时间无法解析和正式时间线冲突均输出诊断。
- 输出结构化缺失、冲突、低置信和失效引用诊断。

验收：作者能查看一次运行将使用和排除的全部事实、计划与约束。

### M2：时间和空间内核

- 实现统一时间坐标、自定义历法、自适应调度和多尺度观察层。
- 实现包含图、传播图、旅行、信息延迟、影响衰减和范围边界。
- 实现检查点、暂停、恢复和确定性重放。

验收：无模型情况下，世界过程能跨日到万年推进并保持可重放。

### M3：人物、势力与修炼裁定

- 实现人物知识、目标、计划、寿命、修炼和物品状态。
- 实现势力战略、预算、多方响应和兴衰生命周期。
- 实现领域命令、规则裁定和确定性策略池。

验收：一个人物和两个势力能在同一地域产生受规则约束的连续互动。

### M4：智能决策和纪元叙事

- 接入四类模型场景和 Host 模型解析。
- 实现候选 Schema、上下文隔离、失败降级和审计记录。
- 实现亿年以上世界、法则和宇宙级纪元叙事。

验收：模型增强与降级运行都能产出合法状态，模型不能绕过内核规则。

### M5：统一文字推演工作台

- 删除世界推演内部菜单、独立实验室/会商页和空间地图视图，接入辅助菜单下的直接二级入口。
- 实现运行切换器、无配置创建、左侧轮次时间线、中央文字舞台、右侧状态检查器、因果抽屉、会商抽屉和分支比较。
- 实现自动投影的章节/剧情工程观察、自然演化对照以及规则来源/裁定依据展示；这些参数均不出现在创建流程中。

验收：作者能在一个工作面完成“创建或切换运行 → 单轮或连续推演 → 阅读世界事件 → 查看状态与因果 → 比较分支 → 选择候选结果”，无需配置世界范围、时间跨度、人物、势力或规则。

### M6：采纳闭环

- 将推演结果转换为人物、势力、物品、地点、设定、修炼和时间线提案。
- 复用统一提案审阅、三方对比、白名单、引用闭合和 Repository 原子保存。

验收：未经作者确认的推演结果不会改变任何正式资料，发生并发修改时可靠中止。

## 25. 最终完成定义

世界推演 V2 完成的标志不是“能点击开始并生成若干事件”，而是：

1. 世界时间能够按照小说自己的历法持续流动。
2. 不同时间尺度产生不同粒度、但属于同一因果链的结果。
3. 人物、势力和世界过程能够基于正式设定自主产生变化。
4. 空间距离、传播、资源、修炼、寿命和知识边界真实约束行动。
5. 作者可以自由决定是否以已有章节为起点，以及主线、故事弧、大纲和章节计划如何约束推演。
6. 单页文字推演舞台能展示轮次、叙事事件、主体状态和因果链之间的连续逻辑，而不依赖空间地图视图。
7. 会商、作者干预和分支比较可以提出行动方案，但不会绕过当前运行的内核裁定或直接修改世界。
8. 所有推演结果都是候选，可重放、可分支、可比较、可审阅，不污染正式事实。
