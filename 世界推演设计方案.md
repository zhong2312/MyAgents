# 世界推演模块设计方案

> **状态**：方案设计 / 首版 AI 接入
> **版本**：V1
> **日期**：2026-08-22
> **依据**：`prototypes/world-simulation-v4`、`Novel-Design.md`、现有人物库/势力库/地点库/时间线契约及用户需求

## 1. 方案结论

世界推演是一个基于正式世界事实的、可复现的沙盒运行时。

正式人物、势力、地点、时间线、世界架构和修炼体系是输入；推演运行产生运行快照、事件账本、状态差异和候选提案。推演不得直接改写正式资料库，也不能把推演结果自动视为历史事实。

原型已经确定了文字叙事舞台、运行分支、三栏工作区和规则结算方向，但当前实现仍使用固定人物、固定势力、固定阈值和内存状态。正式模块需要把这些示例逻辑替换为通用领域模型。

## 2. 核心职责

| 维度     | 模块职责                                                     |
| -------- | ------------------------------------------------------------ |
| 时间     | 维护统一世界时间，决定世界何时进入下一状态                   |
| 空间     | 限定本次推演的观察范围和影响边界                             |
| 人物     | 在时间、地点、认知和能力约束下执行行动                       |
| 势力     | 按目标、资源、关系和战略周期进行决策                         |
| 世界过程 | 独立处理出生、死亡、生产、灾害、节日、秘境、灵气和文明变化   |
| 事件传播 | 判断事件是否通过空间、消息、资源、政治或生态关系影响其它主体 |
| 叙事投影 | 把已裁定的事实转换为观察镜头下的自然语言                     |
| 提案输出 | 将作者认可的结果提交给时间线、人物、势力或剧情工程审阅       |

推演模块不负责直接编辑人物库、势力库、地点库，不负责直接创建正式时间线事实，也不让语言模型替代规则裁定。

## 3. 运行模型

一次推演运行 `SimulationRun` 按以下流程执行：

```text
正式事实快照
  -> 运行配置
  -> 时间调度器
  -> 主体决策
  -> 世界过程
  -> 冲突裁定
  -> 因果传播
  -> 生命周期结算
  -> 事件账本与状态快照
  -> 叙事投影 / 作者提案
```

### 3.1 AI 裁定层边界

正式工作台的“AI 推演”采用规则先行、AI 投影后置的双层链路：

1. 规则内核唯一裁定本轮 `startTime`、`endTime`、轮次 ID、生命周期硬规则和运行状态，并生成可审计的硬事件。
2. 工作台通过模型场景 `simulation.advance` 调用宿主 `context.aiRuns.run()`；模型使用宿主已支持的 `world` 只读上下文模式按需读取人物、势力、世界、时间线和修行事实。`simulation.advance` 是场景 ID，不是新的工具模式。
3. 模型返回 `{ "narrative": "故事正文", "events": [...] }` JSON 候选。`narrative` 是作者主阅读内容，`events` 是辅助账本；候选必须经过本地 Schema、时间窗口、事件类型、实体引用、因果引用、规则引用和传播边界校验，最多保存 8 条。
4. 校验通过后，AI 事件与规则硬事件一起写入当前运行的 `rounds/events` 账本；AI 不得直接写入人物、势力、地点、时间线或正式提案。
5. 模型不可用、超时或本地校验失败时，本轮不写入、不降级为固定模板，界面必须显示真实错误并允许用户重试。模型返回非 JSON 时，工作台最多追加一次无工具的格式整理请求；整理请求只允许依据原始输出返回同一结构或空 `events`，仍须经过本地 Schema 校验，第二次失败即终止本轮。

原型文件 `prototypes/world-simulation-v4/index.html` 仍是离线规则演示，不具备宿主模型能力；只有小说工作台的世界推演视图会发起真实 AI Run。

运行必须记录：

- 基线类型；
- 正式事实源哈希；
- 世界起始时间和终止时间；
- 观察空间；
- 时间尺度；
- 每轮跨度数值与单位；
- 随机种子；
- 推演引擎版本；
- 规则集版本；
- 父运行和分支点；
- 当前运行状态；
- 最近一次已提交的检查点。

同一份基线、同一配置、同一随机种子和同一引擎版本，必须得到相同结果。

## 4. 时间模型

### 4.1 时间坐标

使用独立的 `WorldTime`，不直接依赖本地历法：

```ts
type WorldTime = {
  tick: string;
  calendarId: string | null;
  label: string;
  precision: "exact" | "range" | "approximate" | "unknown";
};
```

`tick` 使用任意精度整数的十进制字符串，避免在千亿年、万亿年尺度出现数值精度损失。正式时间线可以使用现有的 `worldSortKey` 保存可投影的世界时间坐标。

### 4.2 自适应调度

运行配置中的总推演范围和每轮时间跨度分别保存为“数值 + 单位”。用户可选择：天、月、年、万年、千万年、亿年。运行时统一换算为内部世界天数坐标；已有运行记录缺少跨度数值时按 1 兼容读取。总范围只决定运行终点，每轮跨度只决定调度器的最大窗口，二者不可混用。运行清单同时保留总范围的原始数值与单位，避免重新打开运行时只能看到换算后的天数。

尺度不是固定步长，而是“最大窗口 + 聚合策略”。

```text
nextBoundary = min(
  下一个有意义事件边界,
  当前尺度允许的最大窗口,
  运行终点
)
```

调度器只寻找以下边界：

- 已开始行动的完成时间；
- 旅行、消息、资源和灾害的到达时间；
- 修炼、恢复、突破、衰老和寿命边界；
- 势力议事、征税、动员和继承周期；
- 资源生产、消耗、耗尽和恢复节点；
- 节日、秘境、灵气潮汐、季节和灾变周期；
- 观察范围内出现新的可观察主体；
- 当前尺度的最大聚合边界。

没有其它事件时，才推进到尺度上限，不能逐日空循环。

### 4.3 尺度策略

| 尺度   | 主要主体             | 输出方式                     |
| ------ | -------------------- | ---------------------------- |
| 日     | 人物、队伍、地点     | 行动、旅行、修炼、小规模冲突 |
| 月     | 人物、组织、城池     | 战役、密谋、突破、贸易       |
| 年     | 人物、势力、国家     | 国战、政局、外交、人物际遇   |
| 百年   | 代际、势力、区域     | 换代、兴衰、迁移             |
| 千年   | 势力、文明、修炼传统 | 王朝更替、传承断层           |
| 万年   | 文明、生态、世界机制 | 灵气、地貌、文明周期         |
| 千亿年 | 世界、法则、宇宙结构 | 法则漂移、格局重组           |
| 万亿年 | 宇宙和终局机制       | 宇宙生灭、法则耗散           |

周期性事件必须使用区间计数。例如“一年一次节日”在千年窗口中生成一条聚合事件，并记录发生次数，而不是生成一千条逐年事件。

## 5. 空间模型

空间不是地图画布，而是推演范围和传播网络。

空间来源：

- `world/setting-library/spatial-tree.json`：世界架构空间树；
- `world/locations/index.json`：可叙事地点和地点层级；
- 地点之间的旅行、通信、贸易和生态关系。

运行配置包含：

```ts
type ObservationScope = {
  rootNodeIds: string[];
  locationIds: string[];
  includeDescendants: boolean;
  maxDepth: number | null;
  influencePolicy: "inside-only" | "include-boundary-summary";
};
```

空间规则：

- 范围内主体进行完整推演；
- 范围外主体不逐个展开，只形成统计或边界摘要；
- 事件只有在存在空间重叠、影响半径或明确传播通道时，才能跨空间影响主体；
- 范围外事件可以影响范围内的资源、消息、人口和制度，但必须保留“外部来源”；
- 需要详细观察外部事件时，作者扩展空间范围或创建新的运行分支。

## 6. 人物投影

人物库是正式设定，推演使用独立的 `SimulationCharacterProjection`，不回写人物记录。

人物投影包含：

```text
身份：人物 ID、基线版本、当前地点、世界时间、出生时间、死亡时间、年龄、寿命、存活状态
心理：目标、动机、价值观、性格、优势、弱点、恐惧、内在冲突
认知：确认事实、传闻、猜测、误判、秘密、信息来源和抵达时间
关系：人际关系、势力身份、职位、责任、承诺、债务和可调用资源
修炼：体系、轨道、境界、功法、能力、资源、限制、突破历史
行动：当前行动、开始时间、预计完成时间、成本、前置条件和未完成行动
```

现有人物字段中的 `age`、`baseLifespan`、`lifespanLoss` 等存在文本字段，不能依赖自由文本猜测数值。正式设计增加“运行输入映射”：

- 能严格解析的字段才进入规则；
- 无法解析的字段标记为“缺少结构化输入”；
- 需要作者补充时生成诊断；
- 不得静默使用默认寿命、默认境界或默认地点。

## 7. 势力投影

势力库已有地盘、成员、资产、资源、组织单元、关系和状态快照，可以作为推演输入。运行投影增加：

```text
长期目标：存续、扩张、复仇、统一、守护、逐利
当前策略：短期目标、底线、风险偏好、行动计划、可调动预算
运行指标：治理、经济、军事、民望、合法性、领土完整度、资源库存
组织结构：领袖、组织单元、派系、成员规模、继承规则
关系网络：同盟、敌对、竞争、依附、贸易、外交承诺和历史恩怨
生命周期：兴起、扩张、鼎盛、僵化、衰落、分裂、灭亡、重生
```

现有正式势力状态只有 `active / neutral / declining / dissolved`，不能直接承担完整生命周期。丰富状态只存在于运行投影中；作者采纳结果时才映射为正式势力事件或资料变更提案。

策略池至少包括：

- 战争、动员、扩张、收缩、防御、投降、吞并；
- 结盟、会盟、联姻、背叛、附庸、停战、外交施压；
- 税制、改革、镇压、赈济、迁移、贸易、休养生息；
- 招募、扶植、渗透、暗杀、封锁消息、争夺资源；
- 秘境开发、功法封锁、宗门传承、修炼资源配置。

每个动作必须有前置条件、资源成本、执行时长、影响空间、传播通道、成功/失败结果、风险和下一次唤醒时间。

## 8. 世界过程

世界过程不依赖人物或势力的主动意志，至少包括：

- 人口出生、死亡、迁移和代际更替；
- 生产、消费、贸易和资源枯竭；
- 生态恢复、退化、灾害和物种变化；
- 灵气潮汐、灵脉生灭和修炼体系传播；
- 秘境、节日和周期性世界事件；
- 发明、技术传播和制度变迁；
- 地貌、天体、世界屏障、天道和法则变化。

世界过程规则必须结构化，不能从 Markdown 叙述中直接推断全部逻辑。建议使用结构化规则模板，包含规则 ID、触发类型、时间周期或条件、适用空间、参与主体、前置条件、效果、持续时间、传播通道、优先级、聚合策略、证据来源和不确定性范围。

## 9. 事件独立与传播

事件默认独立，只有满足传播条件时才建立因果边。

| 通道 | 传播条件                               |
| ---- | -------------------------------------- |
| 物理 | 同地点、影响半径重叠或存在旅行路径     |
| 消息 | 存在通信路径，且消息在窗口内到达       |
| 资源 | 共享矿脉、商路、生产链或库存           |
| 政治 | 存在势力关系、领土、权利或外交承诺     |
| 生态 | 共享生态区域或灾害影响范围             |
| 修炼 | 存在灵脉、功法、传承或资源链关联       |
| 认知 | 观察者已经获得消息，或满足公开传播条件 |

传播流程：

1. 每个主体基于上一轮快照独立生成行动意图；
2. 世界过程生成自然事件；
3. 按时间和空间重叠把事件分为冲突组；
4. 冲突组内按规则优先级、能力、资源和行动顺序裁定；
5. 通过传播图计算受影响主体；
6. 只把已经到达的消息写入主体认知；
7. 未到达的传播进入下一次调度边界；
8. 没有共同来源或传播路径的事件保持独立。

## 10. 每轮裁定顺序

每轮使用固定顺序，避免数组顺序或界面选择影响结果：

1. 冻结上一轮状态；
2. 推进到期的行动、旅行、消息和资源过程；
3. 结算周期性世界规则；
4. 生成势力策略和人物行动意图；
5. 检查行动前置条件和资源成本；
6. 处理时间、空间和资源冲突；
7. 应用直接状态变化；
8. 沿传播通道传播影响；
9. 结算出生、死亡、寿命、继承和生命周期；
10. 对超过当前尺度的主体进行聚合；
11. 生成事件账本、状态差异和未完成队列；
12. 生成观察镜头和叙事草稿；
13. 原子保存运行检查点。

## 11. 生命周期与尺度降级

### 11.1 人物

普通人物死亡后立即退出活跃决策。未完成行动按行动契约中止、转交或形成遗产，并生成遗物、家族、门徒、制度、传说或文化影响。后续轮次只能作为记忆、后代、遗产或传闻出现。

高境界人物按修炼体系寿命继续存在，不能因为进入百年或千年尺度被统一清除；必须根据境界、资源、突破条件和世界法则单独结算。

亿年以上，普通人口降级为统计群体。满足纪元级行动者条件的特殊主体继续保留；当主体重新进入观察范围或成为关键事件参与者时，可从统计群体生成候选投影。

### 11.2 势力

```text
兴起 -> 扩张 -> 鼎盛 -> 僵化 -> 衰落
                         |       |
                         v       v
                       分裂     灭亡 -> 重生
```

状态转换必须有资源、人口、领土、继承、战争、派系、传承或制度证据。

## 12. 剧情工程参与模式

| 模式         | 推演基线                                   |
| ------------ | ------------------------------------------ |
| 不使用章节   | 使用正式时间线和当前世界事实               |
| 从章节后继续 | 使用该章节之前的正式事实和已确认正文事实   |
| 从章节前重演 | 使用目标章节之前的基线，目标章节不保证发生 |
| 从章节处分支 | 固定章节前基线，从章节节点创建子运行       |

章节内容只能通过已验证的正文事实进入基线，不能把章节规划或未采纳提案当成既成事实。

运行分支与时间线分支分开：运行分支用于沙盒比较，时间线分支用于作者确认后的正式历史。运行结果采纳后才生成时间线事件候选；人物、势力和世界资料是否更新，分别由各自 Repository 审阅。

## 13. 持久化设计

建议新增独立目录，不与剧情工程旧的 `narrative/simulation-proposals/` 混用：

```text
world/simulations/
|-- index.json
|-- runs/
|   |-- <run-id>/
|       |-- manifest.json
|       |-- baseline/
|       |-- rounds/records/
|       |-- events/records/
|       |-- snapshots/records/
|       |-- aggregates/records/
|       `-- diagnostics.json
`-- proposals/
    `-- <proposal-id>/proposal.json
```

持久化规则：

- `index.json` 只保存运行顺序和路径；
- `manifest.json` 保存运行配置、基线、哈希、种子和当前检查点；
- `events` 是运行历史账本；
- `snapshots` 是可重建的性能检查点，不是第二份正式世界事实；
- `baseline` 保存本次运行实际使用的结构化输入快照；
- 分支使用父运行、父检查点和分支时间，不复制全部数据；
- 保存采用 `expectedContent` 和多文件事务；
- 正式资料变化不会静默混入冻结基线；
- 用户可以继续旧基线，也可以从新事实创建新分支；
- 运行失败必须回滚本轮已经写入的文件。

## 14. 界面结构

保留原型的文字舞台和三栏结构。

### 14.1 设置页

包含运行名称、基线模式、时间起止、时间尺度、观察空间、观察对象、章节上下文、规则集、随机种子、结构化输入诊断和影响范围预览。

### 14.2 推演工作区

左栏显示运行分支、时间轮次、检查点、每轮状态、未完成行动和延迟传播。

中栏显示世界动态、人物行动、势力博弈、生命与代际、普通生活、当前观察镜头和本轮叙事结果。

右栏显示主体投影、状态差异、事件证据、因果链、传播路径、不确定性、规则命中和提案操作。

操作需要明确区分“推演一轮”“推演至下一个边界”“推演若干检查点”“暂停”“创建分支”“生成叙事草稿”和“生成候选提案”。

## 15. 提案与叙事

事件结果分为：

```text
confirmed      规则和事实充分支持
inferred       根据多个事实推导
uncertain      存在多个合理结果
blocked        缺少必要输入，不能裁定
aggregated     长尺度聚合结果
```

叙事层只能使用事件账本中的事实：

- `confirmed` 可以直接叙述；
- `inferred` 必须保留推断语气；
- `uncertain` 必须保留可能性；
- `blocked` 不能生成确定结局；
- `aggregated` 只能写过程和结果，不能伪造中间细节。

普通凡人的人生结局不预置固定文案，而是先确认寿命、婚姻、子女、职业、灾害、迁徙、资源和死亡证据，再由叙事层生成“安稳终老、灾难遇害或漂泊终老”等观察镜头。

所有需要进入正式世界的结果必须通过现有 `WorldProposalReview` 体系，不能由推演界面直接写入时间线、人物库或势力库。

## 16. 建议的实现分期

### 第一阶段：领域契约

- `SimulationRun`、`WorldTime`、`ObservationScope`；
- 事件、传播边、状态差异；
- 规则定义和动作定义；
- 人物、势力、世界过程投影；
- 结构化输入诊断。

### 第二阶段：确定性内核

- 时间调度器；
- 周期聚合；
- 主体行动；
- 冲突裁定；
- 传播图；
- 生命周期；
- 尺度降级；
- 随机种子和重放。

### 第三阶段：持久化和分支

- 运行 Repository；
- 基线快照；
- 检查点；
- 分支和父运行；
- CAS、事务和恢复；
- 运行源哈希。

### 第四阶段：工作台界面

- 设置页；
- 三栏文字舞台；
- 事件账本；
- 主体检查器；
- 因果传播视图；
- 运行切换和分支操作。

### 第五阶段：正式提案和叙事

- 时间线事件提案；
- 新人物、新势力和世界动态提案；
- 章节模式；
- 观察镜头；
- 不确定性叙事；
- 作者审核与采纳。

## 17. 验收标准

- 一日跨度不会误触发年度节日、战争或千年秘境；
- 一年跨度能正确聚合节日，但不会错误生成王朝战争；
- 千年跨度能聚合秘境、节日、战争、资源争夺和代际变化；
- 百亿年、万亿年不会逐年展开；
- 普通人物死亡后不再参与后续行动；
- 高境界人物不会因尺度切换被错误清除；
- 事件没有空间或传播路径时不会影响无关主体；
- 消息在到达前不会进入人物知识；
- 同一配置重复运行结果一致；
- 正式人物、势力、时间线和世界架构文件不会被直接修改；
- 外部文件变化能被检测；
- 分支运行不会污染父运行；
- 事件因果关系可追溯；
- 不确定结果不会被叙事层写成确定事实；
- 运行中断后可以从最近检查点继续。

## 18. 用户可见的推演解释与结果

### 18.1 推演如何推理

工作台不展示模型的隐式思维链，而展示可以复核的“裁定链”。每一轮固定经过以下步骤：

```text
冻结基线
  -> 找到最近的有意义时间边界
  -> 推进到期行动、旅行、消息和资源过程
  -> 命中结构化世界规则
  -> 人物按目标、动机、认知、资源和约束提出行动
  -> 势力按战略、预算、关系和风险偏好提出行动
  -> 校验前置条件、成本、时间和空间冲突
  -> 应用状态差异
  -> 通过已有传播通道计算影响与到达时间
  -> 结算寿命、出生、死亡、继承和势力生命周期
  -> 保存事件账本、状态快照、未完成行动和下一唤醒边界
```

规则内核负责时间边界、硬规则和状态不变量；AI 只负责需要语义判断的候选，例如人物在多个可行行动之间的选择、势力策略的解释和传播内容的具体表达。AI 候选必须携带正式主体引用、触发事实、决策、行动、状态变化、因果事件和规则引用，并通过本地 Schema 校验后才能进入运行账本。

### 18.2 推演完成后展示什么

首屏以一段连续的中文世界故事为主，结构化信息退居辅助层。故事应按时间顺序自然叙述本轮变化，至少覆盖命中的人物、冲突与决策、势力反应、世界过程、人民生活，以及新人物或新势力的诞生和后续行动；没有证据的部分必须保留不确定性。结构化信息按“边界、事件、状态、传播”组织，用于点击查看依据：

1. **本轮边界**：起点、终点、停下原因、边界类型、边界来源和下一次唤醒时间。
2. **事件账本**：每条事件的世界时间、主体、地点、标题、结果和确定性。没有具体主体或依据时显示 `blocked` 诊断，不生成泛化故事。
3. **事件详情**：触发事实、主体原状态、认知范围、决策、实际行动、结果、状态差异、因果链、规则命中和未确定部分。
4. **主体状态快照**：人物的地点、年龄、寿命、存活、境界、目标、资源和未完成行动；势力的状态、战略、预算、资源、领地、关系和本轮变化。
5. **传播链**：源事件、传播通道、目标空间、到达时间和当前状态。没有具体源事件或通道时，不显示传播载荷。
6. **叙事投影**：这是主阅读结果。AI 返回 `narrative` 故事正文，按时间顺序连贯描述人物故事、冲突、决策、势力、世界、民生和新主体；事件数组只作为账本和审计依据。`inferred`、`uncertain`、`blocked` 和 `aggregated` 必须在故事语气中保留各自的不确定性。

示例：

```text
第 3 月 12 日 -> 第 3 月 18 日
停下原因：沈照夜的闭关准备完成（行动完成边界）
下一唤醒：第 3 月 22 日，消息“北山封印异常”到达南岭关

沈照夜完成闭关准备
主体：沈照夜 · 地点：北山镇
依据：行动完成时间 = 第 3 月 18 日；目标 = 完成闭关
决策：灵石耗尽前继续闭关
状态变化：currentAction：闭关准备 -> 已完成
确定性：内核裁定
未确定：遭遇、知识变化及对他人的影响等待 AI 候选裁定
```

原型 `prototypes/world-simulation-v4/index.html` 只演示这套规则和展示方式，不调用真实模型；正式工作台才会通过 `simulation.advance` 发起 AI Run。两者都不得把没有证据的模板摘要显示成确定世界事实。
