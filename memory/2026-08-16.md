# 2026-08-16

## MyAgents CLI 实机审计收尾

- 本 Session 对 `dev/0.4.7` 的 CLI bundle authority 优化做了代码审查、自动化验证和 MyAgents 内实机 smoke。结论是薄启动器、App 内唯一 `myagents.cjs`、PATH 优先级与端口选择链路本身成立；CLI/Rust/TypeScript 定向测试通过。
- 实机测试发现 7 个独立问题并提交为 GitHub #531–#537：Global Sidecar 路由、Gemini ACP 清理、Channel uptime、leaf help 的 dry-run、Cron 文档、订阅 Provider verify、未知命令恢复提示。
- 2026-08-16 复查时，#531–#537 均已由仓库 owner 以 `COMPLETED` 关闭并附修复说明；本次记忆审计没有重新执行这些修复的回归测试。详细状态见 `memory/topics/cli-validation.md`。
- Gemini smoke 在无账号环境触发了登录流程；用户明确说明当前没有 Gemini 账号。测试随即停止，遗留的 `gemini --acp` 已精确清理。以后测试外部 Runtime 的 describe/discovery 前，应先确认非交互鉴权状态；可能打开登录流程的命令不应仅因表面只读而直接执行。

## Codex 当前 Session 工具注入核对

- 用户询问的是“当前模型上下文实际注入了哪些 MCP 工具”，不是 MyAgents 已配置的 MCP/产品能力目录。首次回答误把静态展开的工具声明当成完整清单，只识别到 Edge TTS；用户用会话工具开关截图纠正了范围。
- 随后通过 `functions.exec` 内的 `ALL_TOOLS` 实机枚举确认：该 Session 的截图所示能力中，Playwright 有 22 个 MCP 工具、DuckDuckGo 有 2 个、Edge TTS 有 2 个、Cuse 有 24 个，共 4 个 MCP Server / 50 个 MCP 工具；图片理解以原生 `view_image` 暴露，不属于 MCP。另有 122 个 `codex_apps` MCP 工具，但不属于截图中的五项能力。
- 当前 Codex 工具容器会把部分 nested tool 完整展开，另一些只通过 `ALL_TOOLS` 提供名称和描述，并可用 `tools.<name>(args)` 调用。以后回答“本轮能看到/调用哪些工具”时，应枚举 `ALL_TOOLS`，再按 namespace 区分 MCP、原生工具和 App；不能只看静态声明，也不能用 `myagents mcp list` 代替当前 Session 的 effective tool snapshot。
- 协作上，用户明确限定为“注入在你上下文里的工具”后，应直接按该执行层事实回答，不扩展成 MyAgents 产品能力或配置目录；若涉及 UI 开关，还要区分 configured/enabled desired state、Session 固化快照和模型当前可调用 surface。

## AnyDoc 深度体验、性能与责任边界修复

- 2026-08-15 在 `/Users/zhihu/Downloads/test` 完成 dev/0.4.9 AnyDoc 黑盒深测并保留全部生成器、真值、转换结果和分报告；总报告为 `reports/anydoc-deep-test-report.md`。覆盖 13 个格式家族、33 个 accepted jobs、26 份真值样本和 7 份真实样本；26 个 job succeeded、3 个 succeeded with warnings、2 个预期失败、2 个显式取消，成功/失败/取消的发布原子性成立。
- 性能不是当前主阻塞：小型 Office/ODF 通常 4–21 ms worker、约 0.3 s 端到端；88 页原生 PDF 335 ms worker；普通单页 OCR 约 1–3 s，复杂扫描约 3–6 s/页，36 MP 压力图片约 20 s。用户明确要求转换速度进入报告，后续文档能力评测应继续同时给 worker 与端到端两个口径，并解释单 worker 排队影响。
- 深测暴露的责任边界问题已由 `1a57b6cd` 修复：统一日志不再记录正文、密码、原始命令或用户路径预览；CLI 只在 `artifactAvailable` 为真时展示产物，终态不再显示内部 `finalizing`；DocumentProcessingManager 补充最小安全生命周期诊断。该总报告生成于修复前，阅读时不能把这三项继续当作当前未修复缺陷。
- 用户明确把旋转 OCR、扫描表格阅读顺序、PDF URL/金额/软断字、DOCX heading、XLSX 公式/显示格式等 parser/OCR/版面质量问题留待依赖模块升级，本批次只修 owner/日志/状态真值。五 target 签名安装包、资源压力、四 Runtime Skill discovery 与安装版 CLI smoke 仍是 mandatory 发布门槛；`specs/prd/prd_0.4.9_local-document-conversion-ocr.md` 保持 `in-progress`，不能把本机深测或 Batch 2 完成描述为 0.4.9 可发布。

## Managed Codex 子 Agent 生命周期终态修复

- 文档深测期间复现 4 个子 Agent 已完成、durable transcript 中 628 个 nested calls 均 terminal，但 Agent Status Panel 与 `CollabAgent` 卡仍呼吸且 elapsed 继续增长。统一日志证明 child/root `turn/completed` 与持久化均成功，事故窗口无 SSE drop；根因是 Codex adapter gate 只用 child lifecycle 管 root ownership 而未投影父卡 lifecycle，Renderer 被迫把 nested trace `isLoading` 当执行状态。reasoning delta/stop trace suffix 不一致和 archive 后 late event no-op 放大了 terminal burst。
- 独立 PRD `specs/prd/prd_0.4.9_managed-codex-subagent-lifecycle.md` 已实现；提交 `8cb9391f` 以 native child turn 为唯一 authority，增加最小单调父卡 lifecycle、runtime-neutral event 与 critical `chat:subagent-status`，由 external-session/content-blocks 持久化，Renderer/Companion/Panel 共用同一投影；root terminal 只 fail closed，不能用 root success 伪造 child success，queue-only/control-only 卡不获得 lifecycle。
- 同次修复完成 reasoning trace 精确配对、archived message late update、nested trace 收口和 Panel 500 ms 组级 terminal linger。10 个聚焦测试文件 275 项、最终 Codex 85 项、typecheck/lint/server/web build、226 项 classification 检查、三视角 fresh-context review和 Managed Codex 0.146.0 fan-out/interrupt/queue-only/follow-up/reload smoke 均通过；PRD 状态为 `implemented`。
